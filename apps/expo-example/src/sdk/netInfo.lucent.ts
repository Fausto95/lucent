// Generated by scripts/sync-examples.ts from scripts/example-app. Do not edit.
// @react-native-community/netinfo's API, implemented in Lucent per platform
// (the Phase 1 parity port): the Network framework's path monitor on iOS,
// ConnectivityManager and a Lucent NetworkCallback on Android.

export interface NetInfoDetails {
  isConnectionExpensive: boolean;
}

export interface NetInfoState {
  type: string;
  isConnected: boolean;
  isInternetReachable: boolean | null;
  details: NetInfoDetails | null;
}

export declare function fetch(): Promise<NetInfoState>;
/** Calls `listener` with the state now and on each change, until removeEventListener(id). */
export declare function addEventListener(listener: (state: NetInfoState) => void): Promise<number>;
export declare function removeEventListener(id: number): Promise<void>;
