// Generated by scripts/sync-examples.ts from scripts/example-app. Do not edit.

export type Kernel = (n: number) => number;

// React Native provides clock.now(); not every app's types declare it.
const clock = (globalThis as unknown as { performance: { now(): number } }).performance;

/** Best of `runs` timings of `f(n)`, in milliseconds. */
export function timeKernel(f: Kernel, n: number, runs = 3): number {
  let best = Infinity;
  for (let r = 0; r < runs; r++) {
    const start = clock.now();
    f(n);
    best = Math.min(best, clock.now() - start);
  }
  return best;
}
