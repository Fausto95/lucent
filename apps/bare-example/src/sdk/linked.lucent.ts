// Libraries the app already links, bound like the SDK: React Native's own
// pods on iOS, AndroidX on Android. Nothing is added to the app for it.

export declare function linkedLibraries(): Promise<string>;
