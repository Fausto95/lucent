// Generated by scripts/website.ts. Do not edit.

import type { SearchEntry } from "../docs/search";

export const searchIndex: SearchEntry[] = [
 {
  "href": "/docs/",
  "page": "What is Lucent",
  "text": "Lucent compiles TypeScript modules to C++ for React Native: you write a .lucent.ts file, and your app calls it as native code. The TypeScript checker checks the module, Lucent compiles it to C++, and your app links that C++. The import in App.tsx stays an ordinary, typed import. When Metro bundles the app, it swaps the module for a small proxy that calls the C++. No JavaScript engine runs your module, and there's no Swift or Kotlin to write. Modules can also call iOS and Android APIs directly."
 },
 {
  "href": "/docs/#call-ios-and-android",
  "page": "What is Lucent",
  "heading": "Call iOS and Android",
  "text": "A module imports the SDKs directly, typed from your Xcode and Android SDK. Each platform's build compiles its own branch. It's one function of the expo-clipboard port; the examples have the whole port."
 },
 {
  "href": "/docs/#when-to-use-it",
  "page": "What is Lucent",
  "heading": "When to use it",
  "text": "Logic you want in native code: parsers, codecs, geometry, data structures. Calls to iOS and Android APIs, written once in TypeScript instead of in Swift and Kotlin. Work that must stay off the JS thread: async exports run on a background thread."
 },
 {
  "href": "/docs/#when-not-to-use-it",
  "page": "What is Lucent",
  "heading": "When not to use it",
  "text": "Native views. Lucent has none until milestone M3 (roadmap). Production apps. Lucent is experimental, its APIs change without a migration path, and it isn't on npm yet. Code that needs any, eval or dynamic property access. Lucent compiles a checked subset of TypeScript."
 },
 {
  "href": "/docs/#how-it-compares",
  "page": "What is Lucent",
  "heading": "How it compares",
  "text": "Expo Modules and Turbo Native Modules: you write Swift and Kotlin, and a JavaScript API over them. Nitro Modules: you write Swift, Kotlin or C++ against a TypeScript spec. Lucent: you write TypeScript, and the compiler writes the C++ and the calls into the SDK. The comparison has the details, with dates and sources."
 },
 {
  "href": "/docs/install/",
  "page": "Install Lucent",
  "text": "Add one dev dependency, run lucent init to set the app up, and check the machine with lucent doctor. @lucent-lang/lucent isn't on npm yet. Until it is, run pnpm pack in the repository's packages/lucent and install the tarball it writes (roadmap). You need React Native 0.88 or later, or Expo SDK 58 in a development build, and Node 22.12 or later. ### Expo lucent init shows each change as a diff and asks before applying it. In an Expo app, it: - wraps metro.config.js with withLucent, so Metro bundles each module as a proxy; adds the @lucent-lang/lucent config plugin to app.json, which builds your modules during expo prebuild; maps lucent: imports and turns on noUncheckedIndexedAccess in tsconfig.json; adds .lucent/ to .gitignore; writes a first module, src/hello.lucent.ts, if the app has none. > Lucent adds native code, so it doesn't run in Expo Go. Run the app as a development build, with npx expo run:ios or npx expo run:android. ### Bare React Native lucent init shows each change as a diff and asks before applying it. In a bare app, it: - wraps metro.config.js with withLucent, so Metro bundles each module as a proxy; adds a lucent entry to react-native.config.js, so autolinking finds the native package in .lucent/native; applies a Gradle task in android/app/build.gradle that runs lucent build before each Android build; maps lucent: imports and turns on noUncheckedIndexedAccess in tsconfig.json; adds .lucent/ to .gitignore; writes a first module, src/hello.lucent.ts, if the app has none. Run npx lucent init --yes to apply every change without asking. Running it again changes nothing."
 },
 {
  "href": "/docs/install/#check-your-machine",
  "page": "Install Lucent",
  "heading": "Check your machine",
  "text": "It checks Node, React Native, Xcode, CocoaPods, the Android SDK, NDK and JDK, the Metro config and the Gradle task. Each problem comes with its fix. The output above is shortened."
 },
 {
  "href": "/docs/install/#see-lucent-s-errors-in-your-editor",
  "page": "Install Lucent",
  "heading": "See Lucent's errors in your editor",
  "text": "TypeScript accepts code that Lucent rejects, such as any. The plugin shows Lucent's errors as you type; lucent init doesn't add it. In VS Code, pick \"Use Workspace Version\" of TypeScript so the plugin loads."
 },
 {
  "href": "/docs/first-module/",
  "page": "Your first module",
  "text": "In ten minutes: write a module, call it from a screen, change it, and read a compile error. Write the module Save it as src/greet.lucent.ts. Any .lucent.ts file is a module, and its exports are what JavaScript can call. ### Call it from a screen The import is typed from the source, so your editor checks the call. ### Run the app ### Expo The config plugin builds your modules during prebuild. Xcode then compiles the C++ into the app. ### Bare React Native On Android, skip the first two lines: the Gradle task runs lucent build itself. The screen shows Hello, Ada!, computed in C++. ### Change it Change Hello to Hi and save. While Metro runs, lucent dev rebuilds the native package and prints one line in Metro's output: The C++ is part of the app binary, so reloading JavaScript doesn't replace it. Run the app again to see Hi, Ada!. ### Read an error Change the parameter to name: any and save: Each error has a code, a place and a fix. npx lucent explain LUCENT2001 shows why the rule exists, with a wrong and a right example. Put string back."
 },
 {
  "href": "/docs/first-module/#what-happened",
  "page": "Your first module",
  "heading": "What happened",
  "text": "1. The TypeScript checker checked greet.lucent.ts against Lucent's rules. 2. Lucent compiled it to C++ and wrote the native package to .lucent/native. 3. Xcode or Gradle compiled the native package into your app. 4. Metro replaced the greet.lucent import with a proxy. 5. Calling greet went through JSI to the C++ and back. How Lucent works follows each step."
 },
 {
  "href": "/docs/tutorial/1-shared-logic/",
  "page": "1. Measure a trip in shared code",
  "text": "A module that measures distance and speed between GPS fixes, called from a screen. Over eight steps, you'll build a trip tracker: a module that measures a trip from GPS fixes. Start from the app you set up in Install Lucent. Save the module as src/trip.lucent.ts. Fix is an object type. When JavaScript passes a fix, Lucent checks each field is a number, then copies the fix into a C++ struct. radians and EARTH_RADIUS_M aren't exported, so JavaScript never sees them. Only distance and speed cross the boundary."
 },
 {
  "href": "/docs/tutorial/1-shared-logic/#run-it",
  "page": "1. Measure a trip in shared code",
  "heading": "Run it",
  "text": "Build and run the app as in Your first module. The screen shows 3.16 km at 1.3 m/s: the walk from the Eiffel Tower to the Louvre, measured in C++."
 },
 {
  "href": "/docs/tutorial/2-data/",
  "page": "2. Keep the trip in native code",
  "text": "A Trip class that holds its fixes natively, and a summary returned as a copied object. Trip is a class, so JavaScript gets a handle on one native object rather than a copy. Its fixes stay in native code: JavaScript sends each fix once, with add."
 },
 {
  "href": "/docs/tutorial/2-data/#what-s-copied-and-what-isn-t",
  "page": "2. Keep the trip in native code",
  "heading": "What's copied, and what isn't",
  "text": "trip.add(fix) copies the fix. Changing fix in JavaScript afterwards doesn't change the trip. trip.summary() returns a new object on each call: a copy of a TripSummary struct. trip itself crosses by reference. Passing it back to Lucent later gives the same object, with its fixes. trip.count is a getter: reading it calls into native code. This is the shape Design the boundary first recommends: the data stays native, and small values cross."
 },
 {
  "href": "/docs/tutorial/2-data/#run-it",
  "page": "2. Keep the trip in native code",
  "heading": "Run it",
  "text": "The screen shows 5 fixes, 3.55 km, top speed 1.6 m/s."
 },
 {
  "href": "/docs/tutorial/3-async/",
  "page": "3. Simplify the route off the JS thread",
  "text": "An async method that runs on the Lucent thread, and stops when JavaScript aborts it. simplify keeps the fixes that shape the route, and drops those within tolerance meters of a straight line. On a long trip that is slow work, so it's async: it runs on the Lucent thread, and the JS thread stays free. The AbortSignal comes from an AbortController in JavaScript. When the screen goes away, controller.abort() sets the signal, and signal.throwIfAborted() stops the loop with an AbortError. > Aborting doesn't stop Lucent code by itself. It only takes effect where the code checks the signal, or passes it to delay."
 },
 {
  "href": "/docs/tutorial/3-async/#run-it",
  "page": "3. Simplify the route off the JS thread",
  "heading": "Run it",
  "text": "The screen shows Simplifying… for a moment, then 4 fixes shape the route.: one fix sat within 100 meters of the line through its neighbors."
 },
 {
  "href": "/docs/tutorial/4-errors/",
  "page": "4. Throw errors JavaScript can handle",
  "text": "Errors with codes, thrown in Lucent and caught by code in JavaScript. error(code, message) from lucent:core makes an Error with a code. JavaScript receives an ordinary Error, with the same message and code, and a stack whose first frame is the line in trip.lucent.ts that threw. Test the code, not the message: codes are for code, messages are for people. Here, a fix that arrives late is skipped, and any other error still reaches React."
 },
 {
  "href": "/docs/tutorial/4-errors/#run-it",
  "page": "4. Throw errors JavaScript can handle",
  "heading": "Run it",
  "text": "The screen is unchanged. Metro's output shows the warning for the late fix: A fix at 300000 ms comes before the last one, at 2400000 ms."
 },
 {
  "href": "/docs/tutorial/5-platform-code/",
  "page": "5. Read the position from iOS and Android",
  "text": "One module that calls CoreLocation on iOS and LocationManager on Android. One module holds both platforms. Inside if (PLATFORM === \"ios\"), the code uses CoreLocation; in the else, Android's LocationManager. Each platform's build compiles only its own branch. - fromCLLocation uses an iOS type, so it's iOS code and compiles only for iOS. fromLocation is Android code. CLLocationManager is main-thread only on iOS, so it's made inside main(). Its result, a CLLocation, comes back as a promise. Both branches turn SDK objects into a Fix before returning. SDK objects can't cross to JavaScript. import type { Fix } shares the type with the trip module. Android uses the platform's LocationManager, not Play services' fused provider, which the app doesn't include."
 },
 {
  "href": "/docs/tutorial/5-platform-code/#run-it",
  "page": "5. Read the position from iOS and Android",
  "heading": "Run it",
  "text": "Rebuild the app. On a simulator or emulator with a location set, the screen adds where you are. Without a permission, iOS has no position to give, and Android throws a java.lang.SecurityException error, whose message the screen shows. Step 7 asks for the permission."
 },
 {
  "href": "/docs/tutorial/6-callbacks/",
  "page": "6. Send live positions to JavaScript",
  "text": "A CoreLocation delegate and an Android listener that call a JS function with each new position."
 },
 {
  "href": "/docs/tutorial/6-callbacks/#ios-a-delegate",
  "page": "6. Send live positions to JavaScript",
  "heading": "iOS: a delegate",
  "text": "Updates implements CLLocationManagerDelegate. Lucent makes an Objective-C object for it that forwards locationManager(_:didUpdateLocations:) to locationManager_didUpdateLocations: Swift's name, with its labels joined by _."
 },
 {
  "href": "/docs/tutorial/6-callbacks/#android-a-listener",
  "page": "6. Send live positions to JavaScript",
  "heading": "Android: a listener",
  "text": "LocationListener has one method to implement, so a plain function is one. removeUpdates takes the same function back, which is why watch keeps it in listeners."
 },
 {
  "href": "/docs/tutorial/6-callbacks/#to-javascript",
  "page": "6. Send live positions to JavaScript",
  "heading": "To JavaScript",
  "text": "onFix is a JS function. Each update arrives on the Lucent thread and calls it; the call is posted to the JS thread. watch returns an id, and stop(id) removes the manager or listener, which lets go of the callback. > Without stop, the delegate or listener, and the JS callback it holds, are never freed. See When memory is freed."
 },
 {
  "href": "/docs/tutorial/6-callbacks/#run-it",
  "page": "6. Send live positions to JavaScript",
  "heading": "Run it",
  "text": "With a permission granted, move the simulated location: the summary updates with each fix."
 },
 {
  "href": "/docs/tutorial/7-permissions/",
  "page": "7. Ask for the location permission",
  "text": "The permission prompt, the Info.plist entry, and Android permissions added for you."
 },
 {
  "href": "/docs/tutorial/7-permissions/#ios",
  "page": "7. Ask for the location permission",
  "heading": "iOS",
  "text": "requestPermission shows the system's prompt. iOS shows it only if the app says why it needs the location, in Info.plist: ### Expo ### Bare React Native"
 },
 {
  "href": "/docs/tutorial/7-permissions/#android",
  "page": "7. Ask for the location permission",
  "heading": "Android",
  "text": "There's nothing to add to the manifest. The SDK marks requestLocationUpdates with the location permissions it needs, and lucent build adds them to the native package's manifest, which Android merges into the app's. Asking at run time is still up to the app: App.tsx does it with PermissionsAndroid, before watch."
 },
 {
  "href": "/docs/tutorial/7-permissions/#run-it",
  "page": "7. Ask for the location permission",
  "heading": "Run it",
  "text": "Rebuild the app, since Info.plist changed. It asks for the location, then tracks the trip."
 },
 {
  "href": "/docs/tutorial/8-publish/",
  "page": "8. Publish the module as a package",
  "text": "The trip tracker as an npm package that apps install and compile with their own modules. Move src/ into a folder of its own, trip-tracker/, and add these three files. The package ships the modules as source: an app that installs it compiles them with its own modules, into its one native package. - lucent.sources names the folder with the modules. lucent.compatible is the range of Lucent versions the package supports. index.ts re-exports the modules. Metro swaps each for its proxy, as in an app. lucent.json lists what the modules need from the app. Here it's the Info.plist entry, so apps no longer add it by hand. The modules are named trip-tracker/trip and trip-tracker/location, so they can't clash with an app's own trip module. In an app that installs trip-tracker, lucent build finds it and builds its modules. The Expo config plugin writes the Info.plist entry. In a bare app, lucent build names the missing key: You've built a module with shared logic, async work, errors, platform code, callbacks and permissions, and published it. The examples show more modules built the same way."
 },
 {
  "href": "/docs/how-it-works/",
  "page": "How a module becomes native code",
  "text": "Five steps turn a .lucent.ts file into C++ in your app binary, and into a proxy in your JS bundle. The TypeScript checker checks your module Lucent runs the real TypeScript compiler, in strict mode with noUncheckedIndexedAccess. Every type comes from the checker, including narrowing, so your editor and lucent build see the same types. Then Lucent applies its own rules: no any, no var, no dynamic property access. Breaking one stops the build with a LUCENT code, and nothing is written. The diagnostics list every code. ### Lucent writes C++ Each module becomes one C++ file and one header. The #line directives point back to your source, so compiler errors, the debugger and crash reports name .lucent.ts lines. The C++ keeps JavaScript's behavior. A number is a double with JavaScript's arithmetic, strings are UTF-16, and objects are shared by reference. The few differences are listed. ### lucent build writes the native package In .lucent/ What it holds --- --- native/cpp/lucent/, native/cpp/rn/ The C++ runtime, and Lucent, the one TurboModule that serves every module. native/cpp/generated/ Your modules' C++, with a folder per platform when a module has platform code. native/LucentNative.podspec, native/ios/ The CocoaPods spec, and the iOS registration. native/android/ The CMake project, the Gradle file and the Android manifest. native/js/ One proxy per module, and the loader they share. native/types/ Declarations of the lucent: imports, for your editor. native/manifest.json The module list, and a hash of the build's inputs. check.json, android-classpath.json What lucent check and the Android dependency step remember between runs. .lucent/ is generated, so keep it out of git. When nothing changed, lucent build returns at once. Otherwise it rewrites only the files whose content changed. ### Your app build compiles it Autolinking finds the native package through the lucent entry in react-native.config.js. On iOS, CocoaPods compiles it, and it registers itself with React Native when the app loads. On Android, Gradle adds its CMake project to the app's native build. Either way, one C++ TurboModule named Lucent serves every module. There's no codegen step, and no Swift or Kotlin. > The C++ is part of the app binary. After a change, rebuild the app; when modules were added or removed, run pod install first. ### Metro swaps the import for a proxy Your editor and tsc read the .lucent.ts source, so the import is typed. Metro bundles this proxy instead, so none of the module's code is in the JS bundle. If the module was never built, the proxy throws Lucent: greet.lucent.ts has not been compiled. Run lucent build, then rebuild the app."
 },
 {
  "href": "/docs/how-it-works/calls/",
  "page": "How a call reaches native code",
  "text": "A call goes from your JavaScript through a proxy and JSI to your C++. Synchronous calls stay on the JS thread; async ones run on the Lucent thread."
 },
 {
  "href": "/docs/how-it-works/calls/#a-synchronous-call",
  "page": "How a call reaches native code",
  "heading": "A synchronous call",
  "text": "1. The proxy hands the call to the Lucent TurboModule, through JSI. 2. Argument conversion checks each value against its parameter's type, then copies it into C++. 3. Your C++ runs on the JS thread, while JavaScript waits for it. 4. Return conversion turns the C++ result into a JavaScript value. A wrong value never reaches your code. It throws a TypeError that names the function and the argument:"
 },
 {
  "href": "/docs/how-it-works/calls/#an-async-call",
  "page": "How a call reaches native code",
  "heading": "An async call",
  "text": "The arguments are checked and copied on the JS thread, so a wrong value throws at once rather than rejecting. The body runs on the Lucent thread, a background thread, from its first line. The result is posted back to the JS thread, converted, and resolves the promise. A thrown error rejects it instead. All Lucent code runs under one lock, one piece at a time, like JavaScript. A synchronous call waits while async Lucent code runs, until that code reaches an await."
 },
 {
  "href": "/docs/how-it-works/calls/#what-crosses-the-boundary",
  "page": "How a call reaches native code",
  "heading": "What crosses the boundary",
  "text": "Numbers, strings, arrays and objects are copied. Class instances cross by reference and keep their identity, functions become callbacks, and promises stay promises. Type conversions has every type."
 },
 {
  "href": "/docs/how-it-works/platform-calls/",
  "page": "How an SDK call reaches iOS and Android",
  "text": "An SDK call compiles to an Objective-C message send on iOS and a JNI call on Android, typed from the SDKs on your machine. One module holds both platforms. Each platform's build compiles its own branch, and \"See the C++\" shows both results."
 },
 {
  "href": "/docs/how-it-works/platform-calls/#where-the-types-come-from",
  "page": "How an SDK call reaches iOS and Android",
  "heading": "Where the types come from",
  "text": "The first time a module imports lucent:ios/UIKit or lucent:android/android.os, Lucent reads it from your installed Xcode or Android SDK. The result is cached in ~/.cache/lucent for that SDK version, so later builds reuse it. - Android: any package of android.jar, and of the app's Gradle dependencies. Reading the jar takes a few hundred milliseconds. iOS: any framework of the SDK, and of the app's pods. A large framework such as UIKit takes about 40 seconds, once. lucent sdk prefetch reads them ahead of time. Without an SDK, that platform's imports are untyped and lucent build skips the platform, saying so."
 },
 {
  "href": "/docs/how-it-works/platform-calls/#what-a-call-compiles-to",
  "page": "How an SDK call reaches iOS and Android",
  "heading": "What a call compiles to",
  "text": "iOS: a message send with the SDK's own selector, in Objective-C++ compiled against the real headers. Enum values are checked against the SDK at compile time. Android: a JNI call. Class and member IDs are looked up once per call site, and local references are freed after each call."
 },
 {
  "href": "/docs/how-it-works/platform-calls/#what-comes-back",
  "page": "How an SDK call reaches iOS and Android",
  "heading": "What comes back",
  "text": "SDK objects stay in native code. JavaScript never sees them, so return plain values. A Java exception becomes an error whose code is the exception's class. An NSError becomes an error whose code is domain:code. nil or null where the SDK promises a value throws a TypeError. APIs marked main-thread only, such as UIKit's, compile only inside main() (LUCENT3006)."
 },
 {
  "href": "/docs/thinking/three-places/",
  "page": "Three places your code runs",
  "text": "Your code runs in the JS app, crosses the boundary, and runs natively. Values are copied across; class instances, functions and promises keep what they are."
 },
 {
  "href": "/docs/thinking/three-places/#what-crosses-the-boundary",
  "page": "Three places your code runs",
  "heading": "What crosses the boundary",
  "text": "Value Crosses as So --- --- --- number, string, boolean a copy the same value on the other side arrays, object types, Record, Map, Set, Uint8Array, Date a copy changes on one side don't reach the other class instances a reference the same object each time, with its identity functions a callback Lucent can call JavaScript back promises a promise await works in both directions errors an Error with its message and code Copying is what makes the boundary safe: no JavaScript object is shared with native code. It is also what costs time, so large data should cross rarely. SDK objects, such as a CLLocationManager, stay in native code. JavaScript never sees them."
 },
 {
  "href": "/docs/thinking/boundary-first/",
  "page": "Design the boundary first",
  "text": "Decide what JavaScript calls before writing the module: plain data, few calls over data that stays native, async for slow work, errors with codes. Decide what JavaScript calls before writing the module. Each call crosses the boundary, and everything it passes is checked and copied."
 },
 {
  "href": "/docs/thinking/boundary-first/#move-the-work-not-the-data",
  "page": "Design the boundary first",
  "heading": "Move the work, not the data",
  "text": "These are four designs of one computation over 1,000 points, timed in desktop Hermes. chatty makes 1,000 calls with two numbers each, and objects is pathLength, copying 1,000 objects in one call. numbers passes two arrays of numbers instead, and native is spiralLength. Copying 1,000 objects costs more than the computation saves: the call is slower than JavaScript. Numbers are cheaper to copy than objects. Keeping the data in native code wins by far."
 },
 {
  "href": "/docs/thinking/boundary-first/#the-checklist",
  "page": "Design the boundary first",
  "heading": "The checklist",
  "text": "Plain data in and out: numbers, strings, arrays and object types. Never an SDK object. Few calls that do a lot, over data that stays in native code, rather than many small calls. async for anything that may take more than a few milliseconds, so the JS thread stays free. Errors with a code JavaScript can test, from error(code, message). A class when JavaScript needs a handle on native state: its instances cross by reference, not by copy. Measure with lucent bench. It times each case natively and as JavaScript, and fails when the two disagree."
 },
 {
  "href": "/docs/thinking/shared-first/",
  "page": "Write shared code first, platform code last",
  "text": "Keep logic in shared code and SDK calls in small platform branches of the same module. Split into platform files only when the halves share nothing. Put as much as possible in shared code: it is one piece of logic, tested once. Keep platform code to the SDK calls, and turn their results into plain values as soon as you can."
 },
 {
  "href": "/docs/thinking/shared-first/#where-each-piece-goes",
  "page": "Write shared code first, platform code last",
  "heading": "Where each piece goes",
  "text": "No SDK call: shared code, in any module. It also runs in tests on your computer. A few SDK calls in a function: a platform branch, if (PLATFORM === \"ios\") { … } else { … }, in the same module. Helpers, delegates or state that use one SDK: top-level declarations of the same module. They belong to that platform, and compile only there. Two halves that share nothing: platform files, x.ios.lucent.ts and x.android.lucent.ts, behind a declaration file x.lucent.ts. This is the rare case. > A branch needs its else. After if (PLATFORM === \"ios\") return …;, the following code is still shared, so an Android call there fails with LUCENT3004. Both branches are type-checked on every build. A platform whose SDK isn't installed is untyped, and its code is skipped."
 },
 {
  "href": "/docs/thinking/threads/",
  "page": "Which thread your code runs on",
  "text": "Sync exports run on the JS thread, async ones on the Lucent thread, and main() on the main thread. One lock keeps Lucent code from racing itself. Code Runs on --- --- A synchronous export, method or getter the JS thread, while JavaScript waits An async export or method the Lucent thread, a background thread Inside main(() => …) the main thread An SDK callback: a delegate, a listener, a block the Lucent thread, queued, unless the SDK waits for its result A JS callback called from async code the JS thread, posted there"
 },
 {
  "href": "/docs/thinking/threads/#the-one-rule",
  "page": "Which thread your code runs on",
  "heading": "The one rule",
  "text": "Lucent code runs one piece at a time, whatever thread it is on. Every entry into Lucent code holds one lock, and async code lets go of it only at an await. So two pieces of your code never run at once, and there are no data races to guard against. It's the rule JavaScript has, with one difference: a synchronous export waits while async Lucent code runs, until that code reaches an await. Keep long loops in async code, and await between chunks of work."
 },
 {
  "href": "/docs/thinking/threads/#when-you-need-main",
  "page": "Which thread your code runs on",
  "heading": "When you need main()",
  "text": "UIKit and other main-thread APIs compile only inside main(), or in a callback the SDK already calls on the main thread (LUCENT3006). main() returns a promise, so the caller doesn't block. The function you pass can't be async. Android has no such check at compile time. Some Android APIs still need the main thread, such as the clipboard: call them inside main() too."
 },
 {
  "href": "/docs/thinking/memory/",
  "page": "When memory is freed",
  "text": "Objects are freed when their last reference goes. Break cycles, remove delegates and listeners, and close sessions yourself. Objects are freed when the last reference to them goes: Lucent counts references, as Swift does. Most code never notices. Three cases need care."
 },
 {
  "href": "/docs/thinking/memory/#cycles-are-never-freed",
  "page": "When memory is freed",
  "heading": "Cycles are never freed",
  "text": "Two objects that point at each other keep each other alive. Break the cycle when you're done: set one link to null, or keep such links in a Map you clear."
 },
 {
  "href": "/docs/thinking/memory/#delegates-and-listeners-live-until-removed",
  "page": "When memory is freed",
  "heading": "Delegates and listeners live until removed",
  "text": "An SDK object holds on to the delegate or listener you give it, and the listener often holds a JS callback. Give every subscribe an unsubscribe, and remove the SDK registration there too. Debug builds log what's left when the app reloads: [lucent] 2 native reference(s) still held at teardown."
 },
 {
  "href": "/docs/thinking/memory/#close-sessions-and-files-yourself",
  "page": "When memory is freed",
  "heading": "Close sessions and files yourself",
  "text": "Give classes that hold a session, a file or a sensor a close() method, and call it from JavaScript in a finally. JavaScript's garbage collector decides when a JS reference goes, so don't rely on it to release native resources. There's no Weak reference and no using statement yet (roadmap)."
 },
 {
  "href": "/docs/thinking/typescript/",
  "page": "What changes from JavaScript",
  "text": "Lucent is TypeScript whose every value has a native type: no any, no dynamic property access, no eval, and JavaScript's exact number semantics. Lucent compiles every value to a native type known at build time. The rules below follow from that, and each break is a compile error with a LUCENT code. In JavaScript In Lucent Why --- --- --- any a concrete type, a union or a generic (LUCENT2001) Native code needs each value's layout. obj[key] on an object type a Record<string, T> or a Map (LUCENT1001) An object type is a C++ struct with fixed fields. an object literal with methods or getters a class (LUCENT2002, LUCENT1001) Methods live on classes. var let or const (LUCENT1001) One scoping rule. eval, new Function not available (LUCENT1003) No JavaScript engine runs in native code. JSON.parse(text) JSON.parse(text) as Settings The result is checked against the type, and a mismatch throws. throw \"oops\" throw error(\"E_OOPS\", \"Oops\") (LUCENT1006) Only Error values cross the boundary with a message and a code. Promise.race, .then await, Promise.all (LUCENT1003) Not implemented yet. xs.toSorted() [...xs].sort() The ES2023 methods aren't available yet."
 },
 {
  "href": "/docs/thinking/typescript/#numbers-are-exactly-javascript-s",
  "page": "What changes from JavaScript",
  "heading": "Numbers are exactly JavaScript's",
  "text": "A number is a 64-bit float with JavaScript's arithmetic, rounding and formatting: 0.1 + 0.2 is 0.30000000000000004, as in JavaScript. Locals that only hold integers are stored as integers, with the same results. Where Lucent does behave differently from JavaScript, the list of differences has each case and its reason."
 },
 {
  "href": "/docs/coming-from-native/",
  "page": "Coming from Swift or Kotlin",
  "text": "Swift and Kotlin concepts map to Lucent one to one. A module written in Swift, Kotlin and a JS binding becomes one Lucent module. Swift Kotlin Lucent --- --- --- struct data class an object type, type Point = { x: number; y: number }: copied at the boundary class class class: reference-counted, as in Swift protocol interface interface, used with implements a delegate a listener a class that implements the SDK's protocol or interface async suspend async: runs on the Lucent thread @MainActor Dispatchers.Main main(() => …) from lucent:thread T? T? T null throws exceptions throw error(code, message); SDK errors arrive as errors with a code if #available(iOS 17, ) Build.VERSION.SDK_INT >= 34 available(\"ios\", 17), available(\"android\", 34) enum enum class enum SDK names stay as you know them: Swift's names on iOS (UIDevice.current), Java's on Android (Build.MODEL). Nested types join with _, as in UIImpactFeedbackGenerator_FeedbackStyle and Build_VERSION."
 },
 {
  "href": "/docs/coming-from-native/#one-module-instead-of-three-files",
  "page": "Coming from Swift or Kotlin",
  "heading": "One module instead of three files",
  "text": "An Expo module of the clipboard needs Swift, Kotlin and a JavaScript binding. This is a short version of each, in the Expo Modules API: The Lucent module does the same in one file, and JavaScript imports it directly: - The JS binding is gone: the module's exports are the API, typed from the source. main() replaces runOnQueue. APIs that Swift marks @MainActor compile only inside it. Kotlin's !! and ?. become Lucent's ?. and ??, since unannotated Java references are nullable. The full port of expo-clipboard is in the examples."
 },
 {
  "href": "/docs/guides/call-an-ios-api/",
  "page": "Call an iOS API",
  "text": "Import a framework from lucent:ios/ and call it with Swift's names, inside main() when it's main-thread only. Import from lucent:ios/<Framework>: any framework of your Xcode's SDK, or of the app's pods. Names are Swift's. A nested type joins its parts with _: Swift's UIDevice.BatteryState is UIDevice_BatteryState. UIKit is main-thread only, so the calls sit inside main(). Anywhere else they fail to compile with LUCENT3006. Return plain values: SDK objects can't cross to JavaScript. A method with a completion handler is also a promise, under the name Swift gives its async form. An NSError becomes a thrown error. SDK types has every rule. > The first import of a large framework reads it from Xcode, which takes up to a minute once. lucent sdk prefetch does it ahead of time."
 },
 {
  "href": "/docs/guides/call-an-android-api/",
  "page": "Call an Android API",
  "text": "Import a package from lucent:android/ and call it with Java's names, from the app's Context. Import from lucent:android/<package>: any package of android.jar, or of the app's Gradle dependencies. appContext() from lucent:android is the app's Context. A Class<T> parameter takes the class itself: getSystemService(BatteryManager) returns a BatteryManager null. isCharging() is also the property charging, as Kotlin makes properties of getters. References the SDK doesn't mark @NonNull are T null, so check them. A Java exception becomes a thrown error whose code is the exception's class. Nested classes join with _: Build.VERSION is Build_VERSION. SDK types has every rule. > APIs newer than API 24 compile only behind a version check. See Check the OS version."
 },
 {
  "href": "/docs/guides/find-an-sdk-class/",
  "page": "Find an SDK class",
  "text": "lucent sdk search finds classes and members with the import to copy; lucent sdk show prints what Lucent code sees. The search matches classes and members, in the modules you import and those already cached. The import line is ready to copy. To search everything, run lucent sdk prefetch --all once. sdk show prints what Lucent code sees: a class, or one member. Name it as <module>.<Class>, such as UIKit.UIDevice or android.os.BatteryManager. Doc comments say when a class is main-thread only, and the OS version each member needs. Your editor shows the same declarations, since lucent build writes them to .lucent/native/types/."
 },
 {
  "href": "/docs/guides/implement-a-delegate/",
  "page": "Implement a delegate or listener",
  "text": "A class that implements the SDK's protocol or interface, kept alive while the SDK calls it. implements is required: it's what makes a Lucent class usable as the protocol or interface (LUCENT2008). On iOS, a requirement's name is Swift's base name and labels joined with _: locationManager(_:didUpdateHeading:) is locationManager_didUpdateHeading. Implement only the requirements you need. On Android, methods you don't define keep their Java default body. An interface with one method to implement also takes a plain function. The SDK keeps your object while it's the delegate or listener. So does your module: manager and listener stay set until you stop. Callbacks run on the Lucent thread, queued, one at a time with the rest of your Lucent code. A callback whose result the SDK waits for runs at once, on the SDK's thread. On Android, you can also extends an SDK class, calling super() with no arguments. > A delegate or listener lives until it's removed. Give every start a stop, as Send events to JavaScript does."
 },
 {
  "href": "/docs/guides/send-events-to-javascript/",
  "page": "Send events to JavaScript",
  "text": "Take a JS callback, call it from async code, and return an id that stops it. JavaScript passes a function, and Lucent keeps it while tick runs. Each call from async code is posted to the JS thread. start returns an id and stop takes it back. Stopping is what lets go of the function. An event handler called from async code must return void or a Promise. For events that come from the SDK, keep the delegate or listener in a Map by id instead of the Set. Implement a delegate or listener shows one, and the network state example the whole pattern."
 },
 {
  "href": "/docs/guides/accept-a-js-callback/",
  "page": "Accept a JS callback",
  "text": "A function parameter: called at once during a synchronous call, posted to the JS thread from async code. Called from The callback It may return --- --- --- a synchronous export runs at once, on the JS thread any value that can cross; it's checked async code is posted to the JS thread void, or a Promise that Lucent can await - A callback that throws, or whose promise rejects, throws in Lucent too: catch it there. Lucent keeps a callback while it holds a reference to it, and releases it on the JS thread after. After a reload, a stale void callback does nothing; one that returns a value throws."
 },
 {
  "href": "/docs/guides/run-work-off-the-js-thread/",
  "page": "Run work off the JS thread",
  "text": "Make the export async: it runs on the Lucent thread, and its result resolves a promise in JavaScript. An async export runs on the Lucent thread, a background thread, from its first line. Its arguments are checked and copied on the JS thread first, and its result resolves the promise there. - async methods of exported classes run the same way. Lucent code runs one piece at a time. A synchronous call from JavaScript waits while async Lucent code runs, until that code reaches an await. So in long work that JavaScript may call into meanwhile, await delay(0) from lucent:core now and then lets synchronous calls through. > A synchronous export returning a Promise, without async, runs on the JS thread. Mark exports async to move them off it."
 },
 {
  "href": "/docs/guides/cancel-work/",
  "page": "Cancel work",
  "text": "Take an AbortSignal from JavaScript and check it; the promise rejects with an AbortError. Pass an AbortSignal from JavaScript. When JavaScript calls abort(), the signal is set in native code at once. Your code stops where it checks: signal.throwIfAborted(), or a delay(ms, signal). - The rejection is an Error named AbortError, or the reason JavaScript passed to abort(). signal.aborted reads the state, and signal.addEventListener(\"abort\", …) runs a function when it's set. A signal can come into Lucent, but not go back out, and an AbortController can't cross at all."
 },
 {
  "href": "/docs/guides/run-on-the-main-thread/",
  "page": "Run code on the main thread",
  "text": "main(() => …) from lucent:thread runs a function on the main thread and resolves with its result. main(f) from lucent:thread runs f on the main thread and resolves with its result. The caller doesn't block, so call it from async code. - On iOS, APIs that Swift marks @MainActor, such as UIKit's, compile only inside main() (LUCENT3006). So do SDK callbacks the SDK calls on the main thread. f must be a function literal, and it can't be async. Keep it short: move data in and out, and do the work around it. Android has no such check. Some Android APIs still need the main thread, such as the clipboard; call them inside main() too. Objects that deliver to the thread that made them, such as CLLocationManager, should be made inside main()."
 },
 {
  "href": "/docs/guides/throw-and-handle-errors/",
  "page": "Throw and handle errors",
  "text": "error(code, message) throws an error JavaScript receives with its code. Errors from JavaScript and the SDK arrive the same way. error(code, message) from lucent:core makes an Error with a code. Test the code, not the message. JavaScript receives an Error, TypeError or RangeError, with the same name, message and code. Its stack starts at the line that made the error. Only Error values can be thrown (LUCENT1006). Subclasses reach JavaScript as plain errors, with their name, message and code. An async export's error rejects its promise. A JS callback's error, or a rejected promise you await, is thrown in Lucent, with its code. SDK errors arrive the same way: a Java exception's code is its class, and an NSError's is domain:code. An error that nothing catches in an SDK callback is logged: [lucent] uncaught exception in …, in the unified log on iOS and logcat on Android."
 },
 {
  "href": "/docs/guides/check-the-os-version/",
  "page": "Check the OS version",
  "text": "available(\"ios\", 17) and available(\"android\", 31) guard newer APIs. On Android, the compiler requires it. available(\"android\", api) from lucent:android is Build.VERSION.SDK_INT >= api. available(\"ios\", major, minor?) from lucent:ios is Swift's #available. On Android, a class or member newer than API 24 compiles only behind such a check (LUCENT3007). Build_VERSION.SDK_INT >= 31 works too, and so does an early return on the opposite check. On iOS, the compiler doesn't check versions yet: guard new APIs yourself. Each SDK member's doc comment says the version it needs."
 },
 {
  "href": "/docs/guides/share-code-between-platforms/",
  "page": "Share code between iOS and Android",
  "text": "One module with a platform branch per SDK call; platform files only when the halves share nothing. if (PLATFORM === \"ios\") { … } else { … } compiles each branch for its own platform. ? :, switch (PLATFORM) and PLATFORM === \"ios\" && … work too. A top-level function, class or variable that uses one platform's SDK outside a branch belongs to that platform, and compiles only there. Exports run on both platforms, so they branch inside. Both branches are type-checked on every build. Where a platform's SDK isn't installed, its code is untyped and skipped. > A branch needs its else. After if (PLATFORM === \"ios\") return …;, the code that follows is still shared, so an Android call there fails with LUCENT3004."
 },
 {
  "href": "/docs/guides/share-code-between-platforms/#when-to-split-into-platform-files",
  "page": "Share code between iOS and Android",
  "heading": "When to split into platform files",
  "text": "When the two halves share nothing, split the module: x.lucent.ts holds only export declare functions and types, and x.ios.lucent.ts and x.android.lucent.ts each implement all of them (LUCENT3005). JavaScript imports x.lucent either way."
 },
 {
  "href": "/docs/guides/add-permissions-and-config/",
  "page": "Add permissions and native config",
  "text": "Android permissions come from the SDK; iOS usage descriptions go in Info.plist or app.json. What Android iOS --- --- --- permissions an SDK method needs added for you, from the SDK's @RequiresPermission an Info.plist usage description, which you add other permissions and entries the app's AndroidManifest.xml the app's Info.plist asking at run time PermissionsAndroid in JavaScript, or the SDK from Lucent the SDK from Lucent, such as requestWhenInUseAuthorization"
 },
 {
  "href": "/docs/guides/add-permissions-and-config/#android",
  "page": "Add permissions and native config",
  "heading": "Android",
  "text": "When a module calls a method the SDK marks with @RequiresPermission, lucent build adds that permission to the native package's manifest. Android merges it into the app's. Location, network state, vibration and biometrics work this way."
 },
 {
  "href": "/docs/guides/add-permissions-and-config/#ios",
  "page": "Add permissions and native config",
  "heading": "iOS",
  "text": "Expo ### Bare React Native Rebuild the app after changing either file. iOS shows a permission prompt only when the app gives the reason for it. > An app has no lucent.json: that file is for packages, to tell the apps that install them what they need. See Publish a Lucent library."
 },
 {
  "href": "/docs/guides/use-a-third-party-sdk/",
  "page": "Use a third-party SDK",
  "text": "Add the pod or Gradle dependency to the app as usual, then import it from lucent:ios/ or lucent:android/. A library the app already links is imported like the SDK: a pod's module on iOS, an AndroidX package on Android. This module is from the example apps, which add nothing for it."
 },
 {
  "href": "/docs/guides/use-a-third-party-sdk/#add-a-library",
  "page": "Use a third-party SDK",
  "heading": "Add a library",
  "text": "1. Add it to the app as usual: a pod in ios/Podfile, or a dependency in android/app/build.gradle. 2. iOS: run pod install. Lucent reads the pods' headers from the app's Pods folder. 3. Android: nothing more. lucent build asks Gradle for the app's compile classpath, and caches the answer until the Gradle files change. 4. Import from lucent:ios/<Module> or lucent:android/<package>. - Swift Package Manager libraries can't be imported yet; CocoaPods ones can (roadmap). A library in a Lucent package goes in its lucent.json instead, so apps get it when they install the package."
 },
 {
  "href": "/docs/guides/publish-a-library/",
  "page": "Publish a Lucent library",
  "text": "An npm package with a lucent field in package.json, its modules as source, and a lucent.json for its native needs. lucent.sources is the folder with the package's modules. lucent.compatible is the range of Lucent versions it supports; an app outside it fails to build, naming the package. main re-exports the modules. Metro swaps each for its proxy in the app. The package ships sources only. The app compiles them with its own modules, with its own Lucent, into its one native package. lucent.json lists what the modules need from the app: pods, Gradle dependencies, permissions, Info.plist entries. The app's build merges them, and fails if two packages disagree. See lucent.json. Modules are named <package>/<module>, so two packages can each have a storage module. This is the tutorial's package, which step 8 makes. examples/lucent-haptics is another."
 },
 {
  "href": "/docs/guides/use-a-library/",
  "page": "Use a Lucent library",
  "text": "Install it with npm: lucent build compiles its modules with the app's, with their pods, dependencies and permissions. lucent build finds every Lucent package the app depends on, directly or not, and compiles its modules with the app's. Rebuild the app afterwards, after pod install on iOS. - The package's pods, Gradle dependencies and permissions come with it. Info.plist entries it needs are added by the Expo config plugin. In a bare app, lucent build names each missing key: trip-tracker needs NSLocationWhenInUseUsageDescription in ios/App/Info.plist. A package that doesn't support your Lucent version fails the build, and lucent doctor says which. Lucent code can import a package's module too: import { impactAsync } from \"lucent-haptics/src/haptics.lucent\". > The example packages, lucent-haptics and lucent-secure-store, aren't on npm. The example apps install them from the repository, as workspace packages."
 },
 {
  "href": "/docs/guides/port-an-expo-module/",
  "page": "Port an Expo Module",
  "text": "Keep the TypeScript API, and write the Swift and Kotlin bodies as the two branches of one Lucent module. 1. Copy the module's TypeScript API: its exported functions, types and enums. That's your module's export list. 2. For each function, take the Swift and the Kotlin body and write them as the two branches of one Lucent function, if (PLATFORM === \"ios\") { … } else { … }. 3. Keep the SDK calls; change the syntax. Swift's names work as they are, nested types join with _, and Kotlin's getters are properties. 4. Replace AsyncFunction(…).runOnQueue(.main) with an async function whose SDK calls sit in main(). 5. Replace throw Exception(…) with throw error(\"ERR_CODE\", message), keeping the original's codes, so JavaScript can test them. 6. Replace events (sendEvent) with a callback parameter and a stop function. See Send events to JavaScript. 7. Delete the Swift, the Kotlin, the podspec, the Gradle file and expo-module.config.json. Point the JavaScript imports at the .lucent module. Test the port next to the original: call both from one screen and compare. The examples are ports of six Expo and community modules, each checked this way. > Some SDK features aren't bound yet, such as Android generics and iOS subclassing. SDK types lists them; check before you start."
 },
 {
  "href": "/docs/guides/port-a-turbomodule/",
  "page": "Port a TurboModule or Nitro module",
  "text": "The spec becomes the module's exports, hybrid objects become classes, and codegen goes away. TurboModule or Nitro Lucent --- --- the spec: NativeX.ts, or a Nitro .nitro.ts interface the module's exports: its functions, types and classes codegen, or Nitrogen none: lucent build compiles the module Objective-C++, Swift, Kotlin or C++ implementations one .lucent.ts file, with a branch per platform a Nitro hybrid object an exported class: its instances cross by reference Promise<T> methods async exports; they run on the Lucent thread events through RCTEventEmitter or listeners a callback parameter and a stop function ArrayBuffer Uint8Array, copied the library's podspec and Gradle file lucent.json, if the module needs pods, dependencies or permissions Values that Nitro passes without copying, such as large arrays, are copied here. Keep them in native code across calls, as Design the boundary first shows. > Lucent has no views yet: a Fabric component stays as it is (roadmap)."
 },
 {
  "href": "/docs/guides/test-a-module/",
  "page": "Test a module",
  "text": "lucent bench compares native and JavaScript results. A host build runs shared code on your computer, and devices check the SDK calls."
 },
 {
  "href": "/docs/guides/test-a-module/#compare-native-and-javascript",
  "page": "Test a module",
  "heading": "Compare native and JavaScript",
  "text": "lucent bench runs each case as your compiled module and as the same TypeScript run as JavaScript, and fails when the results differ. It needs a desktop Hermes, at ~/hermes or in HERMES_DIR."
 },
 {
  "href": "/docs/guides/test-a-module/#test-platform-code-s-neighbors",
  "page": "Test a module",
  "heading": "Test platform code's neighbors",
  "text": "A host build compiles for your computer. Platform code there throws this code runs only on iOS and Android, so the shared code around it can run in tests."
 },
 {
  "href": "/docs/guides/test-a-module/#run-on-devices",
  "page": "Test a module",
  "heading": "Run on devices",
  "text": "SDK calls run only in the app. Build it for the simulator and the emulator, and check each platform's branch there. > Jest or Vitest can't import lucent:core yet, so shared modules that use it can't run there as TypeScript (roadmap)."
 },
 {
  "href": "/docs/guides/debug-a-crash/",
  "page": "Debug a crash",
  "text": "Errors carry their .lucent.ts line to JavaScript, logs go to the unified log and logcat, and native crashes point at your source."
 },
 {
  "href": "/docs/guides/debug-a-crash/#read-the-error-in-javascript",
  "page": "Debug a crash",
  "heading": "Read the error in JavaScript",
  "text": "An error made in Lucent, with error() or new Error, reaches JavaScript with its first stack frame at the .lucent.ts line that made it."
 },
 {
  "href": "/docs/guides/debug-a-crash/#read-the-logs",
  "page": "Debug a crash",
  "heading": "Read the logs",
  "text": "iOS: Console.app or Xcode's console, filtered on [Lucent]. console.log in a module writes there. Android: adb logcat -s Lucent. An error nothing catches, such as one thrown in an SDK callback, is logged as [lucent] uncaught exception in …."
 },
 {
  "href": "/docs/guides/debug-a-crash/#symbolicate-a-native-crash",
  "page": "Debug a crash",
  "heading": "Symbolicate a native crash",
  "text": "The C++ carries #line directives, so its debug information points at your .lucent.ts files. With the app's dSYM on iOS, or its unstripped .so on Android, a crash report names the module's file and line. Xcode and Android Studio also step through .lucent.ts lines."
 },
 {
  "href": "/docs/guides/debug-a-crash/#check-the-setup",
  "page": "Debug a crash",
  "heading": "Check the setup",
  "text": "Lucent: the native module is not linked means the app was built without the native package: run lucent build, then pod install, and rebuild. module \"x\" is not in the native build means the app is older than the module: rebuild it."
 },
 {
  "href": "/docs/guides/measure-performance/",
  "page": "Measure performance",
  "text": "lucent bench times each case natively and as JavaScript, and shows the speedup. A .bench.ts file next to your modules exports an object of cases: functions without arguments. Each case runs as your compiled module and as the same TypeScript run as JavaScript, in a desktop Hermes. The results must be equal. A speedup below 1x usually means the call copies more data than it computes on. Design the boundary first explains why. Absolute times differ on devices; the comparison carries over. For numbers on a device, time the call in the app's release build."
 },
 {
  "href": "/docs/guides/upgrade/",
  "page": "Upgrade Xcode, the Android SDK or Lucent",
  "text": "A new SDK is read again on the next build. A new Lucent needs lucent doctor, a build, and an app rebuild."
 },
 {
  "href": "/docs/guides/upgrade/#xcode-or-the-android-sdk",
  "page": "Upgrade Xcode, the Android SDK or Lucent",
  "heading": "Xcode or the Android SDK",
  "text": "Nothing to do. SDK types are cached per SDK version, so a new Xcode or android.jar is read again on the next build. Your code is then checked against the new SDK, and errors name what changed. This reads the new SDK's modules ahead of time, instead of during the first build. lucent clean --cache empties the cache, if it takes too much space."
 },
 {
  "href": "/docs/guides/upgrade/#lucent",
  "page": "Upgrade Xcode, the Android SDK or Lucent",
  "heading": "Lucent",
  "text": "lucent doctor checks that every Lucent package in the app supports the new version. Rebuild the app, after pod install on iOS: the C++ runtime is part of the native package. Lucent is experimental, and APIs change without a migration path. Read the release notes first. > There's no way yet to pin an SDK version, or to list the SDK changes that affect your code (roadmap)."
 },
 {
  "href": "/docs/reference/language/",
  "page": "Language features",
  "text": "Every TypeScript feature: whether Lucent supports it, and what to write instead when it doesn't."
 },
 {
  "href": "/docs/reference/language/#declarations-and-modules",
  "page": "Language features",
  "heading": "Declarations and modules",
  "text": "Feature Supported Note --- --- --- export function, export class, export const, export let yes What JavaScript can use. Constants cross once, when the module loads. export type, export interface, export enum yes Enums reach JavaScript as frozen objects. Their values aren't checked at the boundary. Imports between modules, import type yes Relative imports of other .lucent.ts files, and lucent: modules. Imports of npm packages no Only other modules and lucent: (LUCENT3001). export default, export { … }, export from no Export each declaration where it's declared (LUCENT3003). Top-level statements no Only declarations at the top level (LUCENT3002). var no Use let or const (LUCENT1001)."
 },
 {
  "href": "/docs/reference/language/#types",
  "page": "Language features",
  "heading": "Types",
  "text": "Feature Supported Note --- --- --- number, string, boolean, null, undefined yes A number is a double with JavaScript's arithmetic. Object types, interfaces without methods yes Become C++ structs, one per shape. Arrays, tuples, Record<string, T>, Map, Set, Uint8Array, Date, RegExp yes Reference types, shared inside Lucent as in JavaScript. Unions and discriminated unions yes Narrowed by typeof, instanceof, a discriminant field, or a null check. Optional fields and parameters, T null, T undefined yes Generics yes Functions, classes and interfaces. Not exported to JavaScript (LUCENT2007). String literal types yes Checked by TypeScript; at the boundary, any string is accepted. any, and unknown outside catch no Use a concrete type, a union or a generic (LUCENT2001). Intersections, object, bigint, symbol no No native representation (LUCENT2002). Object types with methods or getters no Use a class (LUCENT2002). WeakMap, WeakRef, typed arrays other than Uint8Array no LUCENT2002."
 },
 {
  "href": "/docs/reference/language/#statements-and-expressions",
  "page": "Language features",
  "heading": "Statements and expressions",
  "text": "Feature Supported Note --- --- --- if, switch, for, for…of, for…in, while, do…while, labels yes try, catch, finally, throw yes Only Error values can be thrown (LUCENT1006). Every operator, ??, ?., !, , logical assignment yes ! throws a TypeError when the value is missing. Template literals, spread, destructuring yes Not object rest or computed keys in destructuring (LUCENT1004). obj[key] with a computed key on an object type no Use a Record or a Map (LUCENT1001). delete yes On a Record. Use Map.delete for maps. in yes On a Record only (LUCENT1002). eval, new Function no LUCENT1003. for await no LUCENT1009."
 },
 {
  "href": "/docs/reference/language/#functions-and-classes",
  "page": "Language features",
  "heading": "Functions and classes",
  "text": "Feature Supported Note --- --- --- Function declarations, arrows, closures, recursion yes Closures share the variables they capture. Default and rest parameters yes Not rest parameters in function types (LUCENT2002). Generators, yield, yield yes Not the value of yield, a generator's return value, or async generators. Classes: fields, constructors, methods, getters, setters, static, private, #private yes extends a Lucent class, extends Error, abstract yes implements an interface yes Needed to use a class as the interface (LUCENT2008). Nested classes, static blocks, index signatures on classes no LUCENT1001, LUCENT1005. call, apply, bind, this outside a class no LUCENT1003, LUCENT1001. Object literals with methods, getters or setters no Use a class (LUCENT1001, LUCENT2002)."
 },
 {
  "href": "/docs/reference/language/#async",
  "page": "Language features",
  "heading": "Async",
  "text": "Feature Supported Note --- --- --- async functions and methods, await yes Exported ones run on the Lucent thread. Promise.all, Promise.resolve, Promise.reject, new Promise yes Promise.race, Promise.allSettled, Promise.any, .then no LUCENT1003. AbortSignal, AbortController yes aborted, throwIfAborted(), addEventListener(\"abort\", …). Not signal.reason."
 },
 {
  "href": "/docs/reference/language/#differences-from-javascript",
  "page": "Language features",
  "heading": "Differences from JavaScript",
  "text": "JavaScript Lucent --- --- arr[i] = v past the end makes holes throws a RangeError; append with push values passed to native code are shared copied at the boundary; shared inside Lucent garbage collection frees cycles reference counting never frees a cycle deep recursion throws a RangeError may overflow the native stack console.log(obj) pretty-prints prints String(obj) str.split(regexp) gives undefined for a group that didn't match gives \"\" JSON.parse returns whatever the text holds the text must match the type; a mismatch throws a TypeError naming the path JSON.stringify keeps the parsed text's key order keys follow the declared type's order an abort reason can be any value reasons from JavaScript become errors; abort() in Lucent takes an Error a subclass field read from the base constructor is undefined it reads the type's default: 0, \"\", false any object with the right members fits an interface only classes that declare implements"
 },
 {
  "href": "/docs/reference/built-ins/",
  "page": "Built-ins",
  "text": "The JavaScript built-ins Lucent has, and where they differ from JavaScript's. Built-in What's there Not there, or different --- --- --- Math Every function and constant. Number isInteger, isSafeInteger, isFinite, isNaN, parseInt, parseFloat, the constants; toString(radix), toFixed, toPrecision, toExponential. Globals parseInt, parseFloat, isNaN, isFinite, String(), Number(), Boolean(). String The instance methods, String.fromCharCode, String.fromCodePoint. Case mapping uses Unicode's full rules; localeCompare uses the platform's collator. No Intl. Array The instance methods, including find, findIndex, flatMap, at; Array.from, Array.of, Array.isArray, new Array(n). Not the ES2023 methods: toSorted, toReversed, findLast, findLastIndex. Writing past the end throws a RangeError. Map, Set The whole API; new Map(entries), new Set(iterable). Object keys, values, entries, fromEntries. On records; keys also on object types. JSON JSON.stringify of any value; JSON.parse(text) as T. No replacer, indent or reviver. parse checks the text against T. RegExp Every feature and flag (dgimsuvy); exec, test, match, matchAll, search, replace, replaceAll, split. A replacer with capture parameters needs a literal pattern. Date Constructors, Date.now, Date.parse, Date.UTC, the getters and setters, toISOString, toString and the other string forms. No toLocale… methods. toString has Hermes's format. Uint8Array length, byteLength, subarray, slice, fill, indexOf, includes, set, forEach, map, reduce, join. reduce needs an initial value. Promise Promise.all, Promise.resolve, Promise.reject, new Promise. No race, allSettled, any or .then. console log, info, debug, warn, error: to the unified log on iOS, logcat on Android (tag Lucent). Arguments print as String(value). AbortController, AbortSignal signal, abort(error?); aborted, throwIfAborted(), addEventListener(\"abort\", …). No signal.reason. Not available: Intl, Symbol, WeakMap, WeakRef, Proxy, eval, TextEncoder and TextDecoder. Each is a compile error; for text, use utf8Encode and utf8Decode from lucent:core."
 },
 {
  "href": "/docs/reference/boundary-types/",
  "page": "Types across the boundary",
  "text": "How each type crosses between JavaScript and Lucent: its C++ form, copy or reference, and what JavaScript must pass. TypeScript C++ Crosses as JavaScript must pass --- --- --- --- number double a copy a number boolean bool a copy a boolean string, string literal types lucent::String, UTF-16 a copy a string; any string, for literal types T null, T undefined, x?: T lucent::Opt<T> the value, or its absence null or undefined for absent: both, whatever the type declares T[] lucent::Array<T> a copy an array; each element is checked [A, B] std::tuple<A, B> a copy an array object types, interfaces without methods a struct, by shape a copy an object with each declared field; extra fields are ignored Record<string, V> lucent::Dict<V> a copy an object; each value is checked Map<K, V>, Set<T> lucent::Map, lucent::Set a copy a Map or a Set Uint8Array lucent::Bytes a copy a Uint8Array or an ArrayBuffer Date lucent::Date a copy of its time an object with getTime() RegExp lucent::RegExp its source and flags a RegExp; lastIndex isn't kept enums double or lucent::String a copy a number or a string; membership isn't checked unions std::variant the member JavaScript's value matches a value of one member; object members need a string-literal discriminant (LUCENT2005) classes a reference-counted object a reference, with its identity an instance of that Lucent class interfaces a class implements the class's object a reference an instance of a Lucent class that implements it functions lucent::Fn a callback a function Promise<T> lucent::Promise<T> a promise a promise or any value, as await takes AbortSignal lucent::AbortSignal into Lucent only an AbortSignal Error, TypeError, RangeError lucent::Error its name, message and code any value Iterable<T> (parameters only) lucent::Iter<T> a snapshot, as Array.from takes an iterable void void undefined"
 },
 {
  "href": "/docs/reference/boundary-types/#when-a-value-doesn-t-match",
  "page": "Types across the boundary",
  "heading": "When a value doesn't match",
  "text": "The call throws a TypeError before your code runs, even for an async export. The message names the function, the argument and the path to the bad value:"
 },
 {
  "href": "/docs/reference/boundary-types/#what-can-t-cross",
  "page": "Types across the boundary",
  "heading": "What can't cross",
  "text": "Value Code --- --- SDK objects, such as a CLLocationManager LUCENT2006 iterators and generators, returned LUCENT2006 an AbortSignal going out, and AbortController either way LUCENT2006 generic functions, classes and values LUCENT2007 unions whose members JavaScript can't tell apart LUCENT2005"
 },
 {
  "href": "/docs/reference/boundary-types/#class-instances",
  "page": "Types across the boundary",
  "heading": "Class instances",
  "text": "The same native object is the same JS object each time, so === works. It lives while either side holds it. JavaScript can call new on an exported class. The constructor's arguments are checked like a function's. Public fields, getters, setters and methods are on the prototype. Each read of an array or object field returns a fresh copy. Static methods are on the constructor. Static fields aren't visible from JavaScript. private, protected and #private members are hidden."
 },
 {
  "href": "/docs/reference/boundary-types/#callbacks",
  "page": "Types across the boundary",
  "heading": "Callbacks",
  "text": "Called during a synchronous call from JavaScript, a callback runs at once and may return any value that can cross. Called from async code, it is posted to the JS thread. It must return void, or a Promise that Lucent can await. A JavaScript error thrown in a callback becomes a Lucent error that catch can handle. The function is released when the last Lucent reference to it goes."
 },
 {
  "href": "/docs/reference/platform-types/",
  "page": "SDK types",
  "text": "How iOS and Android SDK declarations appear in Lucent: names, nullability, enums, errors, callbacks."
 },
 {
  "href": "/docs/reference/platform-types/#ios",
  "page": "SDK types",
  "heading": "iOS",
  "text": "In the SDK In Lucent --- --- a framework or pod module import { … } from \"lucent:ios/UIKit\" class and member names Swift's names: UIDevice.current, FileManager.default nested types joined with _: UIImpactFeedbackGenerator_FeedbackStyle methods Swift's base name, labels dropped: evaluatePolicy(policy, reason). Colliding overloads keep their labels: resizeHeight. protocol requirements you implement the base name and labels joined with _: locationManager_didUpdateLocations initializers constructor, labels dropped: new CLLocation(48.85, 2.35) T? T null; nil where the SDK promises a value throws a TypeError enums, C enums enum members, checked against the SDK when the C++ compiles C functions and constants functions and constants: SecItemCopyMatching, kSecClass C structs object types, copied: CGRect is { origin: { x, y }, size: { width, height } } NSString, NSNumber, NSData, NSDate string, number, Uint8Array, Date NSArray<T>, NSSet<T>, NSDictionary<NSString, T> T[], Set<T>, Record<string, T>, copied id, Any ObjCValue as a parameter, NSObject as a result; read it with asString, asNumber, asData… throws (an NSError ) a thrown error whose code is \"domain:code\" an NSError Swift doesn't turn into throws an Out<Error> argument, read after the call a completion handler a promise, under the name of Swift's async form: await context.evaluatePolicy(policy, reason) a block parameter a function a protocol an interface to implements; optional requirements are optional @MainActor only inside main() (LUCENT3006) a weak delegate property kept alive by its owner while set"
 },
 {
  "href": "/docs/reference/platform-types/#android",
  "page": "SDK types",
  "heading": "Android",
  "text": "In the SDK In Lucent --- --- a package of android.jar or of the app's Gradle dependencies import { … } from \"lucent:android/android.os\" class and member names Java's names: Build.MODEL, getSystemService nested classes joined with _: Build_VERSION, ConnectivityManager_NetworkCallback getX() and isX() without arguments also a read-only property: getDefaultVibrator() is defaultVibrator, isCharging() is charging references without a @NonNull annotation T null Class<T> parameters the class itself: getSystemService(Vibrator) arrays byte[], int[], long[], String[] only an exception a thrown error whose code is the exception's class: java.lang.SecurityException an interface with one method to implement a function, or a class that implements it an interface with several a class that implements it; default methods keep their Java body a class to extend class Watcher extends ConnectivityManager_NetworkCallback, calling super() with no arguments APIs newer than API 24 only under available(\"android\", N) or Build_VERSION.SDK_INT >= N (LUCENT3007) @RequiresPermission the permission is added to the native package's manifest"
 },
 {
  "href": "/docs/reference/platform-types/#not-bound-yet",
  "page": "SDK types",
  "heading": "Not bound yet",
  "text": "iOS: pointers, Selector, AnyClass, generic Objective-C classes, protocol compositions, Swift-only APIs, factory initializers, subclassing iOS classes, catching an NSException. Android: generic classes' methods (Consumer<T>), java.util collections, Task<T> as a promise, arrays other than those above, writing Java fields, the current Activity. Both: SDK objects crossing to JavaScript, and calling a method of an SDK object as a value. The roadmap tracks what comes next."
 },
 {
  "href": "/docs/reference/modules/",
  "page": "The lucent: modules",
  "text": "The declarations of lucent:core, lucent:platform, lucent:thread, lucent:ios and lucent:android."
 },
 {
  "href": "/docs/reference/modules/#lucent-core",
  "page": "The lucent: modules",
  "heading": "lucent:core",
  "text": "Helpers for any module: delays, errors with codes, UTF-8, a monotonic clock."
 },
 {
  "href": "/docs/reference/modules/#lucent-platform",
  "page": "The lucent: modules",
  "heading": "lucent:platform",
  "text": "Which platform the code runs on, for platform branches."
 },
 {
  "href": "/docs/reference/modules/#lucent-thread",
  "page": "The lucent: modules",
  "heading": "lucent:thread",
  "text": "The main thread, for platform code."
 },
 {
  "href": "/docs/reference/modules/#lucent-ios",
  "page": "The lucent: modules",
  "heading": "lucent:ios",
  "text": "iOS helpers for platform code. The SDK itself is lucent:ios/<Framework>."
 },
 {
  "href": "/docs/reference/modules/#lucent-android",
  "page": "The lucent: modules",
  "heading": "lucent:android",
  "text": "Android helpers for platform code. The SDK itself is lucent:android/<package>."
 },
 {
  "href": "/docs/reference/cli/",
  "page": "CLI",
  "text": "Every lucent command and flag. Every command finds the .lucent.ts files under the project, skipping node_modules, ios, android and dot-directories, and the modules of the Lucent packages the app depends on. With no command, in a terminal, lucent opens lucent dev in a Lucent project and lucent init elsewhere. Command What it does --- --- lucent build Compile the project's modules and write the native package (.lucent/native); skipped when nothing changed lucent check Type-check and validate every module without writing anything lucent dev Rebuild on every change: a live dashboard of modules, platforms and problems (one line per build outside a terminal) lucent doctor Check the machine and the app: Node, React Native, Xcode, CocoaPods, the Android SDK, the JDK, the Metro config, versions lucent init Set an app up for Lucent: the Metro config, the Expo plugin or the Gradle task, tsconfig.json, .gitignore, a first module lucent new module Scaffold a module in src/: shared, or with --ios / --android one module that branches on PLATFORM lucent explain What a LUCENT diagnostic code means, and how to fix it (every code without one) lucent bench Time the cases of your .bench.ts files natively and as JavaScript, and show the speedup (needs a desktop Hermes) lucent clean Remove the generated .lucent/ (the next build starts over) lucent sdk search Find SDK classes and members by name, with the import to copy (your imports and the SDK cache) lucent sdk show Print the declaration Lucent code sees for an SDK type or member: android.os.Vibrator, UIKit.UIDevice.current lucent sdk prefetch Extract SDK bindings into the cache ahead of use (default: the lucent: modules the project imports) lucent sdk coverage Per SDK module, the members Lucent code can call: idiomatic, raw, unrepresentable ### Flags every command takes Flag Meaning --- --- --root <dir> Project directory (default: the current one) --json Machine-readable output, for tools and CI --help Show help Output is in color and animated only in a terminal. NO_COLOR, FORCE_COLOR, CI and TERM=dumb are respected. The tables on this page are generated from the command table lucent --help reads."
 },
 {
  "href": "/docs/reference/cli/#lucent-build",
  "page": "CLI",
  "heading": "lucent build",
  "text": "Compile the project's modules and write the native package (.lucent/native); skipped when nothing changed Flag Meaning --- --- --force Rebuild even when nothing changed --platforms <list> Targets for platform code: ios, android, host (default: the SDKs installed) --out <dir> Where to write the native package (default: .lucent/native) - When nothing changed since the last build, it says so and writes nothing, unless --force. Otherwise it rewrites only the files whose content changed. next says what the app needs: a rebuild (after pod install when files were added or removed), a reload, or nothing. A platform whose SDK isn't installed is skipped, with a warning. --platforms host builds stubs whose platform code throws, for tests. On a problem, nothing is written and the exit code is 1."
 },
 {
  "href": "/docs/reference/cli/#lucent-check",
  "page": "CLI",
  "heading": "lucent check",
  "text": "Type-check and validate every module without writing anything It reports problems as the diagnostics page shows them, and writes nothing. A pass is remembered, so checking an unchanged project takes a fraction of a second. Use it in CI."
 },
 {
  "href": "/docs/reference/cli/#lucent-dev",
  "page": "CLI",
  "heading": "lucent dev",
  "text": "Rebuild on every change: a live dashboard of modules, platforms and problems (one line per build outside a terminal) Flag Meaning --- --- --compact One line per build instead of the dashboard (Metro runs it so) In a terminal, it shows a dashboard of modules, platforms and problems. Its keys rebuild (r), clear the cache (c), run the doctor (d), open a problem in your editor (o) and quit (q). With --compact, or outside a terminal, it prints one line per build, as above: that's what withLucent runs next to Metro."
 },
 {
  "href": "/docs/reference/cli/#lucent-doctor",
  "page": "CLI",
  "heading": "lucent doctor",
  "text": "Check the machine and the app: Node, React Native, Xcode, CocoaPods, the Android SDK, the JDK, the Metro config, versions Each check passes, warns or fails, with its fix. The exit code is 1 when one fails. It doesn't load the compiler, so it answers even when the project doesn't build."
 },
 {
  "href": "/docs/reference/cli/#lucent-init",
  "page": "CLI",
  "heading": "lucent init",
  "text": "Set an app up for Lucent: the Metro config, the Expo plugin or the Gradle task, tsconfig.json, .gitignore, a first module Flag Meaning --- --- --yes Apply every change without asking It shows each change as a diff and applies the ones you confirm; --yes applies them all. Install Lucent lists the changes for Expo and bare apps. Running it again changes nothing."
 },
 {
  "href": "/docs/reference/cli/#lucent-new-module",
  "page": "CLI",
  "heading": "lucent new module",
  "text": "Scaffold a module in src/: shared, or with --ios / --android one module that branches on PLATFORM Flag Meaning --- --- --ios Implement the iOS branch (without --android, the Android branch throws) --android Implement the Android branch (without --ios, the iOS branch throws) --shared One module for every platform (the default)"
 },
 {
  "href": "/docs/reference/cli/#lucent-explain",
  "page": "CLI",
  "heading": "lucent explain",
  "text": "What a LUCENT diagnostic code means, and how to fix it (every code without one) Prints a code's entry from the diagnostics: why the rule exists, the fix, and a wrong and a right example. lucent explain 3006 works too."
 },
 {
  "href": "/docs/reference/cli/#lucent-bench",
  "page": "CLI",
  "heading": "lucent bench",
  "text": "Time the cases of your .bench.ts files natively and as JavaScript, and show the speedup (needs a desktop Hermes) Each case runs as your module compiled to C++ and as the same TypeScript run as JavaScript, in a desktop Hermes. The results must match. It needs Hermes at ~/hermes, or HERMES_DIR; Design the boundary first shows its output."
 },
 {
  "href": "/docs/reference/cli/#lucent-clean",
  "page": "CLI",
  "heading": "lucent clean",
  "text": "Remove the generated .lucent/ (the next build starts over) Flag Meaning --- --- --cache Also remove the SDK bindings cache"
 },
 {
  "href": "/docs/reference/cli/#lucent-sdk-search",
  "page": "CLI",
  "heading": "lucent sdk search",
  "text": "Find SDK classes and members by name, with the import to copy (your imports and the SDK cache)"
 },
 {
  "href": "/docs/reference/cli/#lucent-sdk-show",
  "page": "CLI",
  "heading": "lucent sdk show",
  "text": "Print the declaration Lucent code sees for an SDK type or member: android.os.Vibrator, UIKit.UIDevice.current"
 },
 {
  "href": "/docs/reference/cli/#lucent-sdk-prefetch",
  "page": "CLI",
  "heading": "lucent sdk prefetch",
  "text": "Extract SDK bindings into the cache ahead of use (default: the lucent: modules the project imports) Flag Meaning --- --- --ios [<modules>] iOS modules, comma-separated; alone: every module --android [<packages>] Android packages, comma-separated; alone: every package --all Every module of every installed SDK"
 },
 {
  "href": "/docs/reference/cli/#lucent-sdk-coverage",
  "page": "CLI",
  "heading": "lucent sdk coverage",
  "text": "Per SDK module, the members Lucent code can call: idiomatic, raw, unrepresentable Flag Meaning --- --- --ios <modules> iOS modules, comma-separated --android <packages> Android packages, comma-separated (p. for a prefix) --check <baseline> Fail when the unrepresentable share grows past a baseline JSON"
 },
 {
  "href": "/docs/reference/lucent-json/",
  "page": "lucent.json",
  "text": "What a Lucent package's platform code needs from the app: pods, Gradle dependencies, permissions, Info.plist entries. What a Lucent package's platform code needs from the app that installs it. It sits next to the package's package.json. The app's build merges it with its other Lucent packages'; two packages that disagree on a value fail the build. Field Type Meaning --- --- --- ios object What the iOS code needs. ios.pods { [key]: string } CocoaPods the native package depends on: pod name → version requirement, as in a Podfile (\"~> 1.0\"). Their frameworks can be imported from lucent:ios/ once installed. ios.infoPlist { [key]: string } Info.plist entries: key → string value. The Expo config plugin adds them, unless the app sets the key itself. For bare apps, lucent build names the keys the app's Info.plist lacks. android object What the Android code needs. android.dependencies { [key]: string } Gradle artifacts: \"group:artifact\" → version. They become api dependencies of the native package, so lucent:android/ can import their classes. android.permissions string[] Android permissions for the manifest, such as \"android.permission.CAMERA\". Permissions the SDK methods you call require are added without listing them here. Only packages have a lucent.json: an app's own is not read. An app adds its pods, Gradle dependencies, permissions and Info.plist entries the usual way. Its pods and Gradle dependencies can be imported from lucent:ios/ and lucent:android/ too. The schema ships in the package, at @lucent-lang/lucent/schemas/lucent.schema.json, and this table is generated from it."
 },
 {
  "href": "/docs/reference/metro-and-expo/",
  "page": "Metro, Expo and editor options",
  "text": "The options of withLucent, the Expo config plugin and the editor plugin."
 },
 {
  "href": "/docs/reference/metro-and-expo/#metro-withlucent",
  "page": "Metro, Expo and editor options",
  "heading": "Metro: withLucent",
  "text": "Option Default Meaning --- --- --- watch on for start, run:ios and run:android Runs lucent dev --compact next to the dev server, which rebuilds the native package on each save. LUCENT_WATCH=0 or 1 sets it when watch isn't given. - Each .lucent.ts file is bundled as its proxy, .lucent/native/js/<module>.js. A module that was never built bundles an error: Lucent: <file> has not been compiled. Run lucent build and rebuild the app. The watcher prints one line per build in Metro's output and never reads Metro's keys. It stops with Metro. withLucent goes in front of the config's Babel transformer, or React Native's or Expo's default one."
 },
 {
  "href": "/docs/reference/metro-and-expo/#expo-the-config-plugin",
  "page": "Metro, Expo and editor options",
  "heading": "Expo: the config plugin",
  "text": "The plugin takes no options. During expo prebuild, it: - runs lucent build once; a failed build stops prebuild with the errors; creates react-native.config.js with the lucent entry, or stops and names the entry to add if the file exists without it; applies the Gradle task that runs lucent build before each Android build (Groovy build.gradle only); adds the Info.plist entries that Lucent packages list in their lucent.json, unless the app sets the key itself. Lucent adds native code, so Expo Go can't load it. Use a development build: npx expo run:ios, npx expo run:android or EAS Build."
 },
 {
  "href": "/docs/reference/metro-and-expo/#editor-plugin",
  "page": "Metro, Expo and editor options",
  "heading": "Editor plugin",
  "text": "lucent init adds the first two; the plugin line is yours to add. The plugin shows Lucent's errors, with their fix and code, in .lucent.ts files. In VS Code, pick \"Use Workspace Version\" of TypeScript so it loads."
 },
 {
  "href": "/docs/reference/diagnostics/",
  "page": "Diagnostics",
  "text": "Every LUCENT code, with its reason, its fix, and a wrong and a right example. lucent build, lucent check, lucent dev and the editor plugin report the same diagnostics. A build with one writes nothing. lucent explain <code> prints the explanation this page shows. Codes are stable. 1xxx are syntax and built-ins, 2xxx types without a native form, 3xxx modules and platform code. 9001 is a TypeScript error: Lucent compiles only programs that type-check. Code Meaning --- --- LUCENT1001 Syntax outside the subset, such as var, getters in object literals, or an async generator. LUCENT1002 An operator used outside what the subset supports, such as delete, in on anything but a record, or instanceof with a generic class. LUCENT1003 A built-in function or method Lucent does not implement, such as Symbol(), eval or an unknown Math, Number or string method. LUCENT1004 A destructuring form outside the subset: object rest, computed keys, or a pattern without an initializer. LUCENT1005 A class feature outside the subset, such as extending a built-in other than Error, or an override that changes the native signature. LUCENT1006 Throwing a value that is not an Error. LUCENT1007 A call Lucent cannot compile, such as spread arguments outside rest parameters, or a platform class without a constructor binding. LUCENT1008 An assignment to something that cannot be assigned, such as an unknown field or a function. LUCENT1009 A loop over a value that is not iterable in Lucent, or for await. LUCENT2001 any, or unknown outside a catch clause: every value needs a native type. LUCENT2002 A type with no native representation: intersections, bigint, symbol, object, WeakMap, Intl, or an index signature mixed with properties. LUCENT2003 An object used as a type with a different shape; object types must match exactly to share a native representation. LUCENT2004 A collection used where its element type would change (A[] as (A B)[]); annotate the value with the target type. LUCENT2005 A union JavaScript values cannot be told apart by at the boundary; add a string-literal discriminant. LUCENT2006 A value that cannot cross the JavaScript boundary, such as a generator, a match result, an AbortSignal or a platform object. LUCENT2007 A generic function or class exported to JavaScript; export a concrete wrapper. LUCENT2008 A value used as an interface its class does not declare with implements. LUCENT2009 A class member whose native signature differs from the interface member it implements. LUCENT3001 An import from something other than another .lucent.ts file or a built-in lucent: module. LUCENT3002 A top-level statement that is not a declaration. LUCENT3003 An export form Lucent does not support: export lists, re-exports, default exports. LUCENT3004 A platform SDK import in a file whose platform cannot use it, an SDK module without bindings, or platform code used outside its platform. LUCENT3005 Platform implementations that do not match their shared declaration file. LUCENT3006 A main-thread-only platform API used outside main(() => …). LUCENT3007 A platform API newer than the oldest supported OS version, used without an available() or SDK_INT check around it. LUCENT9001 A TypeScript error. Lucent stops at type errors, because it compiles from the checker's types."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1001",
  "page": "Diagnostics",
  "heading": "LUCENT1001",
  "text": "Syntax outside the subset Lucent compiles a subset of TypeScript whose every construct has a native equivalent with the same behaviour. var (function scoping and hoisting), with, labels on blocks, accessors in object literals and async generators fall outside it. Fix: rewrite it with the supported form: let/const for var, a class for an object with accessors."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1002",
  "page": "Diagnostics",
  "heading": "LUCENT1002",
  "text": "Unsupported operator Objects in Lucent have a fixed native layout, so an operator that adds or removes properties at run time (delete) has no native equivalent. in works on records (Record<string, T>), whose keys are dynamic, and not on objects with a known shape. Fix: use a Map or a Record for keys that come and go, or an optional field for one that may be missing."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1003",
  "page": "Diagnostics",
  "heading": "LUCENT1003",
  "text": "Built-in without a native implementation Every built-in a module calls runs as native code from the Lucent runtime. The ones listed in the language reference are implemented with JavaScript's exact semantics; the rest are reported rather than approximated. Fix: use a built-in Lucent implements, or write the helper in the module."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1004",
  "page": "Diagnostics",
  "heading": "LUCENT1004",
  "text": "Unsupported destructuring Object rest ({ a, ...rest }) would need a new object type, made of the remaining fields at run time. Computed keys would need a dynamic lookup. Neither has a fixed native layout. Fix: name the fields you need, or pass the whole object."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1005",
  "page": "Diagnostics",
  "heading": "LUCENT1005",
  "text": "Unsupported class feature Classes compile to C++ classes. A subclass of Map or Array would inherit the runtime's container internals. An override whose parameters or result differ from the base method's can't share its native slot. Fix: hold the built-in in a field instead of extending it, and keep overrides' signatures the same as the base method's."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1006",
  "page": "Diagnostics",
  "heading": "LUCENT1006",
  "text": "Throwing a value that is not an Error Errors cross into JavaScript as Error objects with a name, a message and a stack that points at the Lucent source. A thrown string or number has none of that and no native type to catch it by. Fix: throw new Error(…), a built-in error such as TypeError, or a class that extends Error."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1007",
  "page": "Diagnostics",
  "heading": "LUCENT1007",
  "text": "Call Lucent cannot compile Native calls pass a fixed number of arguments of known types. Spreading an array into ordinary parameters has no native form. Neither does a platform API called as a promise where it has none, or a platform class without an initializer. Fix: pass the arguments one by one, or declare the callee with a rest parameter."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1008",
  "page": "Diagnostics",
  "heading": "LUCENT1008",
  "text": "Assignment to something that cannot be assigned Assignments write to a native variable or field. A function, an import, or a field the value's type does not have is not one. Fix: assign a variable or a field the type declares."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent1009",
  "page": "Diagnostics",
  "heading": "LUCENT1009",
  "text": "Loop over a value that is not iterable for…of works on arrays, strings, maps, sets, typed arrays and generators, and for…in on records. Async iteration (for await) is not supported: await each promise in an ordinary loop. Fix: loop over an array (Object.keys, Array.from), or await inside a plain loop."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2001",
  "page": "Diagnostics",
  "heading": "LUCENT2001",
  "text": "Value without a native type Native code needs to know each value's layout at compile time. any gives it none, and unknown only stands for a caught error, whose type JavaScript does not say. Fix: use a concrete type, a union, or a generic parameter."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2002",
  "page": "Diagnostics",
  "heading": "LUCENT2002",
  "text": "Type without a native representation Each type maps to one native representation. An intersection can combine unrelated layouts, bigint and symbol are not implemented yet, and object says nothing about the layout. Fix: spell the combined type out as one object type, or use a concrete type."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2003",
  "page": "Diagnostics",
  "heading": "LUCENT2003",
  "text": "Object used as a type with a different shape Object types with the same fields share one native struct. TypeScript lets a value with more fields stand for a type with fewer. Natively they are different structs, and the extra fields would have nowhere to go. Fix: pass exactly the fields the type declares, or widen the parameter's type."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2004",
  "page": "Diagnostics",
  "heading": "LUCENT2004",
  "text": "Collection used with another element type An array of numbers and an array of number string have different native element types. One can't be used as the other: writes through the wider type wouldn't fit. Fix: annotate the collection with the element type it is used as."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2005",
  "page": "Diagnostics",
  "heading": "LUCENT2005",
  "text": "Union JavaScript values cannot be told apart by A value from JavaScript carries no type, so Lucent decides which union member it is from the value itself. Object members need a string-literal field, such as kind, whose value names the member. Fix: add a string-literal field such as kind: \"circle\" to each object member."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2006",
  "page": "Diagnostics",
  "heading": "LUCENT2006",
  "text": "Value that cannot cross the JavaScript boundary Exports convert their parameters and results between JavaScript and native values. Some native values have no JavaScript form: a generator's state, a platform object, a signal made in native code. Fix: return plain data (an array instead of a generator, fields instead of a platform object)."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2007",
  "page": "Diagnostics",
  "heading": "LUCENT2007",
  "text": "Generic export Generics compile to C++ templates, instantiated for the types the module uses. JavaScript calls an export without types, so there is no instantiation to call. Fix: keep the generic function private and export concrete wrappers."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2008",
  "page": "Diagnostics",
  "heading": "LUCENT2008",
  "text": "Interface not declared with implements An interface with methods compiles to an abstract C++ base class, and only classes that declare implements derive from it. A class that matches the interface only by shape, or an object literal, is not one of them. Fix: add implements to the class."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent2009",
  "page": "Diagnostics",
  "heading": "LUCENT2009",
  "text": "Method signature differs from the interface's The implementing method overrides the interface's native method, so it needs the same parameter and result types. TypeScript accepts compatible variations (an optional parameter, a narrower result) that are different native signatures. Fix: declare the method with exactly the interface's parameter and result types."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent3001",
  "page": "Diagnostics",
  "heading": "LUCENT3001",
  "text": "Import from outside Lucent Everything a module runs is compiled to native code, so it can only use other Lucent modules and the built-in lucent: modules (lucent:core, lucent:thread, lucent:platform, the SDKs). An npm package or a plain .ts file has no native implementation. Fix: move the code into a .lucent.ts module, or use a lucent: module."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent3002",
  "page": "Diagnostics",
  "heading": "LUCENT3002",
  "text": "Statement at the top level A module's top level holds declarations only: functions, classes, types and variables. Statements that run when the module loads have no native place to run, since native modules are created on first use. Fix: move the statement into a function, or into a variable's initializer."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent3003",
  "page": "Diagnostics",
  "heading": "LUCENT3003",
  "text": "Unsupported export form Each export becomes a property of the module's native object, named by its declaration. Export lists, re-exports and default exports name exports apart from their declarations. Fix: put export on the declaration itself."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent3004",
  "page": "Diagnostics",
  "heading": "LUCENT3004",
  "text": "Platform SDK import where it cannot be used lucent:ios/ modules exist on iOS and lucent:android/ on Android. A shared module uses them inside if (PLATFORM === \"ios\") (or the Android branch), and a .ios.lucent.ts file only its own platform's. Fix: use the SDK inside a PLATFORM branch, or in the platform's own file."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent3005",
  "page": "Diagnostics",
  "heading": "LUCENT3005",
  "text": "Platform files that do not match their declaration A module split into name.ios.lucent.ts and name.android.lucent.ts declares its exports in name.lucent.ts. Every platform file must export exactly the declared functions, with compatible types, so JavaScript sees one module. Fix: export the same functions, with the declared types, from every platform file."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent3006",
  "page": "Diagnostics",
  "heading": "LUCENT3006",
  "text": "Main-thread API off the main thread Lucent code runs on its own thread. UIKit, and other APIs the SDK marks main-thread only, must be called on the main thread. main(() => …) from lucent:thread runs a function there, and resolves with its result. Fix: wrap the call in main(() => …) from lucent:thread."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent3007",
  "page": "Diagnostics",
  "heading": "LUCENT3007",
  "text": "Platform API newer than the oldest supported OS Apps run on older OS versions than the SDK they build with. An API introduced later crashes there, so Lucent requires a check that the running OS has it. Fix: check first: if (available(\"android\", 26)) … or Build_VERSION.SDK_INT >= 26."
 },
 {
  "href": "/docs/reference/diagnostics/#lucent9001",
  "page": "Diagnostics",
  "heading": "LUCENT9001",
  "text": "TypeScript error Lucent compiles only programs that type-check: every value's native type comes from the TypeScript checker. The message is TypeScript's own. Fix: fix the type error; your editor shows the same message."
 },
 {
  "href": "/docs/reference/compatibility/",
  "page": "Compatibility",
  "text": "The React Native, Expo, Node, JDK, Android and iOS versions Lucent supports. Supported --- --- React Native 0.88 or later, New Architecture Expo SDK 58 or later, in a development build Node 22.12 or later JDK, for Android builds 17 to 21 Android API 24 and later; newer APIs need an available check iOS React Native's minimum iOS version Xcode, Android SDK and NDK the versions your React Native version needs lucent doctor checks these on your machine, and this table is generated from the same numbers. Tested on the iOS simulator (Xcode 27) and the Android emulator, in the example apps, bare and Expo. Not yet on physical devices (roadmap)."
 },
 {
  "href": "/docs/examples/",
  "page": "Examples that call iOS and Android",
  "text": "Ports of popular Expo and community modules, each one Lucent module for both platforms, running in the repository's example apps. Example Replaces What to look at --- --- --- Clipboard expo-clipboard The smallest, to start with: one SDK class per platform. Location expo-location A CoreLocation delegate, an Android listener, and updates sent to a JS callback. Network state @react-native-community/netinfo The Network framework's C API with a block; an Android class extended in Lucent. Local authentication expo-local-authentication A completion handler awaited as a promise, and NSError out-parameters. Secure store expo-secure-store Keychain C functions, the Android Keystore, and a module shipped as a package. Haptics expo-haptics Main-thread UIKit calls, and Android API-level checks. Each is one module for both platforms. They run in the repository's example apps, bare and Expo. On the iOS simulator and the Android emulator, they pass the same checks as the packages they replace. They haven't been tested on physical devices yet (roadmap)."
 },
 {
  "href": "/docs/examples/clipboard/",
  "page": "Clipboard",
  "text": "expo-clipboard's text API on UIPasteboard and Android's ClipboardManager. iOS: UIPasteboard.general, read and written like a property. Android: ClipboardManager, called inside main() because Android lets only the focused app read the clipboard. error(code, message) from lucent:core throws an error JavaScript can tell apart by its code. Source: clipboard.lucent.ts."
 },
 {
  "href": "/docs/examples/location/",
  "page": "Location",
  "text": "expo-location's API on CoreLocation and Android's LocationManager: permission, current position, and positions sent to JavaScript as they arrive. iOS: Updates implements CLLocationManagerDelegate. The managers are made on the main thread with main(), since they deliver to the thread that made them. Android: a plain function is the LocationListener, and removeUpdates takes the same function back. watchPositionAsync sends each position to a JS callback until stopWatching(id). Android's location permissions reach the app's manifest by themselves: the SDK marks requestLocationUpdates with them. > The port reads the permission but doesn't ask for it. Ask with PermissionsAndroid or expo-location, and add NSLocationWhenInUseUsageDescription to Info.plist. Android uses the platform's LocationManager, not Play services' fused provider. Generic Java interfaces such as Consumer<Location> aren't bound yet, so the module listens for one fix instead of calling getCurrentLocation. Source: location.lucent.ts."
 },
 {
  "href": "/docs/examples/netinfo/",
  "page": "Network state",
  "text": "@react-native-community/netinfo's API on the Network framework and Android's ConnectivityManager, with change events sent to JavaScript. iOS: the Network framework's C functions. The path monitor's update handler is a Lucent function passed as a block, called on the main queue. Android: Watcher extends ConnectivityManager.NetworkCallback, an SDK class, and overrides three of its methods. addEventListener sends each change to a JS callback and returns an id that removeEventListener takes back. Source: netInfo.lucent.ts."
 },
 {
  "href": "/docs/examples/local-authentication/",
  "page": "Local authentication",
  "text": "expo-local-authentication's API on LAContext and Android's BiometricManager: hardware, enrollment and a Face ID or Touch ID prompt. iOS: await context.evaluatePolicy(policy, reason). The SDK's completion handler becomes a promise, under the name Swift gives its async form. iOS: canEvaluatePolicy reports why it can't with an NSError out-parameter, read through Out<Error>. Android: the platform's BiometricManager and KeyguardManager, with API-level checks. > Android has no biometric prompt yet: it needs the current activity, which lucent:android doesn't expose (roadmap). Source: localAuthentication.lucent.ts."
 },
 {
  "href": "/docs/examples/secure-store/",
  "page": "Secure store",
  "text": "expo-secure-store's API on the iOS Keychain and the Android Keystore, shipped as a Lucent package. iOS: the Keychain's C functions (SecItemAdd, SecItemCopyMatching), with a query built as a Record and the result read through Out<NSObject>. Android: an AES/GCM key from the Android Keystore encrypts each value, stored in SharedPreferences. It ships as an npm package, lucent-secure-store: an app that installs it compiles the module with its own. Source: examples/lucent-secure-store."
 },
 {
  "href": "/docs/examples/haptics/",
  "page": "Haptics",
  "text": "expo-haptics' API on UIKit's feedback generators and Android's Vibrator, shipped as a Lucent package. iOS: UIKit's feedback generators may only run on the main thread, so each call is inside main(). The compiler rejects them anywhere else. Android: available(\"android\", 31) picks VibratorManager or Vibrator. Calling an API newer than the app's minimum without such a check is a compile error. It ships as an npm package, lucent-haptics, with the same API as expo-haptics. Source: examples/lucent-haptics."
 },
 {
  "href": "/docs/comparison/",
  "page": "Lucent, Expo Modules, Nitro and Turbo Native Modules",
  "text": "What each one has you write, how it reaches native code, what it can do today, and when to pick it. Expo Modules, Nitro Modules, Turbo Native Modules and Lucent all let JavaScript call native code synchronously over JSI. They differ in the languages you write, how the interface is declared, and how much of the platform you reach today. Native code of any of these kinds needs a development build: it doesn't run in Expo Go. How a module becomes native code shows what Lucent generates."
 },
 {
  "href": "/docs/comparison/#performance",
  "page": "Lucent, Expo Modules, Nitro and Turbo Native Modules",
  "heading": "Performance",
  "text": "Lucent calls go through JSI into C++, like Nitro Modules and C++ Turbo Native Modules. We publish no speed comparison between these tools. lucent bench measures your own module against the same code run as JavaScript."
 },
 {
  "href": "/docs/comparison/#when-to-pick-each",
  "page": "Lucent, Expo Modules, Nitro and Turbo Native Modules",
  "heading": "When to pick each",
  "text": "Expo Modules: your team already writes Swift and Kotlin, or you need views and full platform SDK access today. Nitro Modules: you want the most performance, write C++, Swift or Kotlin, and like declaring the interface in TypeScript first. Turbo Native Modules: you are writing a library that should depend only on React Native core. Lucent: you want native code without adding Swift, Kotlin or C++ to the codebase. You can do without views and an npm release for now."
 },
 {
  "href": "/docs/comparison/#sources",
  "page": "Lucent, Expo Modules, Nitro and Turbo Native Modules",
  "heading": "Sources",
  "text": "The other tools were last checked on 2026-09-23, and Lucent's rows updated on 2026-09-24. Corrections are welcome. - Expo Modules overview Expo Module API Expo modules in bare apps Add custom native code What is Nitro? Nitro Hybrid Objects Nitrogen Nitro View Components Nitro minimum requirements Turbo Native Modules Cross-platform C++ modules Swift in native modules Fabric Native Components"
 },
 {
  "href": "/docs/faq/",
  "page": "Frequently asked questions",
  "text": "Short answers to the questions people ask first, each with a link to the details."
 },
 {
  "href": "/docs/faq/#can-i-use-lucent-in-production",
  "page": "Frequently asked questions",
  "heading": "Can I use Lucent in production?",
  "text": "Not yet. It's experimental, its APIs change without a migration path, and it hasn't been tested on physical devices. The roadmap tracks what's left."
 },
 {
  "href": "/docs/faq/#does-it-work-in-expo-go",
  "page": "Frequently asked questions",
  "heading": "Does it work in Expo Go?",
  "text": "No: Lucent adds native code, which Expo Go can't load. Use a development build, with npx expo run:ios or EAS Build (Install Lucent)."
 },
 {
  "href": "/docs/faq/#is-my-module-s-code-in-the-js-bundle",
  "page": "Frequently asked questions",
  "heading": "Is my module's code in the JS bundle?",
  "text": "No. Metro bundles a small proxy instead, and the code runs as C++ in the app binary (How a module becomes native code)."
 },
 {
  "href": "/docs/faq/#can-a-module-import-npm-packages",
  "page": "Frequently asked questions",
  "heading": "Can a module import npm packages?",
  "text": "Only other modules, lucent: modules, and Lucent packages' modules. JavaScript code around it can import anything, as usual (Language features)."
 },
 {
  "href": "/docs/faq/#is-it-faster-than-javascript",
  "page": "Frequently asked questions",
  "heading": "Is it faster than JavaScript?",
  "text": "For work on data that stays in native code, often by a lot. Copying large data across the boundary can cost more than it saves: measure with lucent bench (Design the boundary first)."
 },
 {
  "href": "/docs/faq/#can-i-call-any-ios-or-android-api",
  "page": "Frequently asked questions",
  "heading": "Can I call any iOS or Android API?",
  "text": "Most of them, typed from your installed SDKs. Some aren't bound yet, such as Android generics and Swift-only APIs (SDK types)."
 },
 {
  "href": "/docs/faq/#do-i-still-need-xcode-and-android-studio",
  "page": "Frequently asked questions",
  "heading": "Do I still need Xcode and Android Studio?",
  "text": "You need their SDKs, to build the app and to type the SDK imports. You don't write Swift, Kotlin or Objective-C (Compatibility)."
 },
 {
  "href": "/docs/faq/#how-do-i-debug-a-module",
  "page": "Frequently asked questions",
  "heading": "How do I debug a module?",
  "text": "Errors reach JavaScript with their .lucent.ts line, and native crashes point at your source (Debug a crash)."
 },
 {
  "href": "/docs/faq/#can-i-publish-a-library-written-in-lucent",
  "page": "Frequently asked questions",
  "heading": "Can I publish a library written in Lucent?",
  "text": "Yes: an npm package that ships its modules as source, compiled by each app that installs it (Publish a Lucent library)."
 },
 {
  "href": "/docs/faq/#how-does-it-compare-with-nitro-or-expo-modules",
  "page": "Frequently asked questions",
  "heading": "How does it compare with Nitro or Expo Modules?",
  "text": "They have you write Swift, Kotlin or C++; Lucent has you write TypeScript. The comparison has the details."
 },
 {
  "href": "/docs/roadmap/",
  "page": "Roadmap",
  "text": "What works today, what's next, and what's later, milestone by milestone. Lucent is experimental. Its APIs change without a migration path, and it hasn't been tested on physical devices. Don't ship it in a production app yet."
 },
 {
  "href": "/docs/roadmap/#m0-foundations",
  "page": "Roadmap",
  "heading": "M0: Foundations",
  "text": "A module runs as C++ in a bare React Native app and an Expo app, on iOS and Android. Status Item --- --- done A C++ runtime with JavaScript's numbers, strings, arrays, maps, sets and errors. done One C++ TurboModule, autolinked on iOS and Android. done The compiler: the TypeScript checker, then C++ with #line back to the source. done lucent build, the Metro integration and the Expo config plugin. done Every language feature tested against the same code run as JavaScript."
 },
 {
  "href": "/docs/roadmap/#m1-the-language",
  "page": "Roadmap",
  "heading": "M1: The language",
  "text": "Self-contained modules, such as parsers, codecs and data structures. Status Item --- --- done Statements, expressions, destructuring and template literals. done Object types, unions, narrowing, enums and generics. done Classes with inheritance and interfaces, closures, generators, regular expressions and JSON. done async and await off the JS thread, AbortSignal, JS callbacks and promises. done Lucent code runs one piece at a time, so it has no data races."
 },
 {
  "href": "/docs/roadmap/#m2-platform-apis",
  "page": "Roadmap",
  "heading": "M2: Platform APIs",
  "text": "Call the iOS and Android SDKs directly from Lucent. Status Item --- --- done SDK types read from your Xcode and Android SDK on first import, then cached. done One module for both platforms, with platform branches; platform files as an option. done Delegates, listeners and blocks; completion handlers as promises. done Android classes extended in Lucent, and Android version checks at compile time. done iOS main-thread-only APIs checked at compile time. done Libraries the app links: its pods on iOS, its Gradle dependencies on Android. done Lucent packages on npm, with lucent.json for their native needs. done Ports of nine Expo and community modules, checked against the originals. next Android generics, such as Consumer<T>, and java.util collections. next Task<T> and ListenableFuture<T> as promises, and the current Activity. later Swift-only and Kotlin-only APIs, and subclassing iOS classes. later iOS version checks at compile time. later Weak references, and using for sessions and files. later Swift Package Manager libraries. later Pinning an SDK version, and listing what an SDK update changes for your code. later Rarer SDK types, such as pointers and selectors, as ports need them."
 },
 {
  "href": "/docs/roadmap/#m3-views",
  "page": "Roadmap",
  "heading": "M3: Views",
  "text": "Native views from Lucent components. Status Item --- --- later SwiftUI or UIKit views and Compose or Android views, on Fabric."
 },
 {
  "href": "/docs/roadmap/#m4-production",
  "page": "Roadmap",
  "heading": "M4: Production",
  "text": "Ready for apps in production. Status Item --- --- in progress @lucent-lang/lucent on npm; today it builds and installs from its tarball in CI. done Incremental builds, and rebuilds as you edit. done Crashes and errors that point at .lucent.ts lines. done Lucent's errors in the editor. in progress Performance budgets in CI. next Testing on physical devices; today, simulators and emulators. next A JavaScript lucent:core, so Jest and Vitest can run shared modules."
 },
 {
  "href": "/docs/roadmap/#not-planned",
  "page": "Roadmap",
  "heading": "Not planned",
  "text": "Running JavaScript in native code: there's no JavaScript engine there. Reflection, eval and prototypes. Freeing reference cycles by itself. This page is generated from ROADMAP.md in the repository."
 }
];
