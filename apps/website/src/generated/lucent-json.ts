// Generated by scripts/website.ts. Do not edit.

/** From packages/lucent/schemas/lucent.schema.json. */
export const lucentJsonDescription = "What a Lucent package's platform code needs from the app that installs it. It sits next to the package's package.json. The app's build merges it with its other Lucent packages'; two packages that disagree on a value fail the build.";

export const lucentJsonFields: { field: string; type: string; description: string }[] = [
  {
    "field": "ios",
    "type": "object",
    "description": "What the iOS code needs."
  },
  {
    "field": "ios.pods",
    "type": "{ [key]: string }",
    "description": "CocoaPods the native package depends on: pod name → version requirement, as in a Podfile (\"~> 1.0\"). Their frameworks can be imported from lucent:ios/* once installed."
  },
  {
    "field": "ios.infoPlist",
    "type": "{ [key]: string }",
    "description": "Info.plist entries: key → string value. The Expo config plugin adds them, unless the app sets the key itself. For bare apps, lucent build names the keys the app's Info.plist lacks."
  },
  {
    "field": "android",
    "type": "object",
    "description": "What the Android code needs."
  },
  {
    "field": "android.dependencies",
    "type": "{ [key]: string }",
    "description": "Gradle artifacts: \"group:artifact\" → version. They become api dependencies of the native package, so lucent:android/* can import their classes."
  },
  {
    "field": "android.permissions",
    "type": "string[]",
    "description": "Android permissions for the manifest, such as \"android.permission.CAMERA\". Permissions the SDK methods you call require are added without listing them here."
  }
];
