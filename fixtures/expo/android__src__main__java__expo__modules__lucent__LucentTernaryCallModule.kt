// Generated by Lucent. Do not edit.

package expo.modules.lucent

import expo.modules.kotlin.functions.Coroutine
import expo.modules.kotlin.jni.ArrayBuffer
import expo.modules.kotlin.modules.Module
import expo.modules.kotlin.modules.ModuleDefinition
import expo.modules.kotlin.records.Field
import expo.modules.kotlin.records.Record

class LucentTernaryCallModule : Module() {
  override fun definition() = ModuleDefinition {
    Name("Lucent_ternary-call")

    Function("choose") { flag: Boolean ->
      choose(flag)
    }

    Function("nested") { flag: Boolean, other: Boolean ->
      nested(flag, other)
    }
  }

  private fun pick(value: Double): Double {
    return value * 2.0
  }

  private fun choose(flag: Boolean): Double {
    val chosen: Double = if (flag) pick(1.0) else pick(2.0)
    return chosen
  }

  private fun nested(flag: Boolean, other: Boolean): Double {
    return if (flag) pick(3.0) else (if (other) pick(4.0) else 5.0)
  }
}
