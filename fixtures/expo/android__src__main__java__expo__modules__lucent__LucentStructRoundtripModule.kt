// Generated by Lucent. Do not edit.

package expo.modules.lucent

import expo.modules.kotlin.functions.Coroutine
import expo.modules.kotlin.jni.ArrayBuffer
import expo.modules.kotlin.modules.Module
import expo.modules.kotlin.modules.ModuleDefinition
import expo.modules.kotlin.records.Field
import expo.modules.kotlin.records.Record

class LucentStructRoundtripModule : Module() {
  class User(
    @Field var id: String = "",
    @Field var age: Int = 0,
    @Field var nickname: String? = null,
    @Field var tags: MutableList<String> = mutableListOf()
  ) : Record

  override fun definition() = ModuleDefinition {
    Name("Lucent_struct-roundtrip")

    Function("birthday") { user: User ->
      birthday(user)
    }

    Function("describe") { user: User ->
      describe(user)
    }
  }

  private fun birthday(user: User): User {
    return User(id = user.id, age = user.age + 1, nickname = user.nickname, tags = user.tags)
  }

  private fun describe(user: User): String {
    val nickname: String? = user.nickname
    if (nickname == null) {
      return user.id + " (" + lucentStr(user.age) + ")"
    }
    return nickname!! + " aka " + user.id
  }
}
