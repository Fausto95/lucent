// Generated by Lucent. Do not edit.

package expo.modules.lucent

import expo.modules.kotlin.functions.Coroutine
import expo.modules.kotlin.jni.ArrayBuffer
import expo.modules.kotlin.modules.Module
import expo.modules.kotlin.modules.ModuleDefinition
import expo.modules.kotlin.records.Field
import expo.modules.kotlin.records.Record

class LucentKitchenModule : Module() {
  class Stats(
    @Field var count: Int = 0,
    @Field var labels: Map<String, String> = emptyMap(),
    @Field var values: MutableList<Double> = mutableListOf()
  ) : Record

  override fun definition() = ModuleDefinition {
    Name("Lucent_kitchen")

    Function("summarize") { stats: Stats, key: String, verbose: Boolean ->
      summarize(stats, key, verbose)
    }
  }

  private fun label(stats: Stats, key: String): String {
    val found: String? = stats.labels[key]
    if (found != null) {
      return found!!
    }
    return "none"
  }

  private fun summarize(stats: Stats, key: String, verbose: Boolean): String {
    var out: String = label(stats, key)
    var i: Int = 0
    while (i < stats.count) {
      out = out + "!"
      i = i + 1
    }
    var evens: MutableList<Double> = mutableListOf<Double>()
    for (v in stats.values) {
      if (v % 2.0 == 0.0 && v > 0.0) {
        evens.add(v)
      } else {
        if (v < 0.0) {
          break
        } else {
          continue
        }
      }
    }
    if (!verbose || evens.size.toDouble() == 0.0) {
      return out
    }
    return out + " " + lucentStr(evens.size.toDouble()) + " " + lucentStr(-i)
  }
}
