// Generated by Lucent. Do not edit.

package expo.modules.lucent

import expo.modules.kotlin.functions.Coroutine
import expo.modules.kotlin.jni.ArrayBuffer
import expo.modules.kotlin.modules.Module
import expo.modules.kotlin.modules.ModuleDefinition
import expo.modules.kotlin.records.Field
import expo.modules.kotlin.records.Record

class LucentWorkerThreadModule : Module() {
  override fun definition() = ModuleDefinition {
    Name("Lucent_worker-thread")

    AsyncFunction("summarize") Coroutine { values: MutableList<Double> ->
      summarize(values)
    }

    AsyncFunction("label") Coroutine { count: Double ->
      label(count)
    }
  }

  private suspend fun summarize(values: MutableList<Double>): Double {
    return kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.Default) {
      var total: Double = 0.0
      for (value in values) {
        total = total + value
      }
      return@withContext total
    }
  }

  private suspend fun label(count: Double): String {
    return kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.Main) {
      return@withContext "count: " + lucentStr(count)
    }
  }
}
