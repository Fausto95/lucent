// Generated by Lucent. Do not edit.

package com.margelo.nitro.lucent

import androidx.annotation.Keep
import com.facebook.proguard.annotations.DoNotStrip
import com.margelo.nitro.core.ArrayBuffer
import com.margelo.nitro.core.Promise

@Keep
@DoNotStrip
class HybridNativeUnion : HybridNativeUnionSpec() {
  override fun evaluate(value: Double): NativeUnionResult {
    val result = NativeUnionBodies.evaluate(value)
    return result.toNitro()
  }

  override fun read(result: NativeUnionResult): Double {
    val result = NativeUnionBodies.read(NativeUnionBodies.Result.fromNitro(result))
    return result
  }
}

object NativeUnionBodies {
  data class Result(
    var kind: String,
    var value: Double?,
    var message: String?
  ) {
    fun toNitro(): NativeUnionResult = NativeUnionResult(kind = kind, value = value, message = message)

    companion object {
      fun fromNitro(value: NativeUnionResult): Result = Result(kind = value.kind, value = value.value, message = value.message)
    }
  }

  fun evaluate(value: Double): Result {
    if (value < 0.0) {
      return Result(kind = "error", value = null, message = "Negative")
    }
    return Result(kind = "ok", value = value, message = null)
  }

  fun read(result: Result): Double {
    if (result.kind == "ok") {
      return result.value!!
    }
    return 0.0
  }
}
