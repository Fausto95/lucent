// Generated by Lucent. Do not edit.

import Foundation
import NitroModules

class HybridWorkerThread: HybridWorkerThreadSpec {
  func summarize(values: [Double]) throws -> Promise<Double> {
    return Promise.async {
      let result = try await WorkerThreadBodies.summarize(values: values)
      return result
    }
  }

  func label(count: Double) throws -> Promise<String> {
    return Promise.async {
      let result = try await WorkerThreadBodies.label(count: count)
      return result
    }
  }
}

enum WorkerThreadBodies {
  static func summarize(values: [Double]) async throws -> Double {
    return try await Task.detached {
      var total: Double = 0.0
      for value in values {
        total = (total + value)
      }
      return total
    }.value
  }

  @MainActor static func label(count: Double) async throws -> String {
    return "count: " + lucentStr(count)
  }
}
