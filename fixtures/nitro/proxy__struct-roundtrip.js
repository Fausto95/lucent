// Generated by Lucent. Do not edit.

import { NitroModules } from "react-native-nitro-modules";
import { lucentCall } from "@lucent-lang/runtime";

const native = NitroModules.createHybridObject("StructRoundtrip");

export function birthday(user) {
  const result = lucentCall(() => native.birthday({ ...user, nickname: (user.nickname == null ? undefined : user.nickname) }));
  return { ...result, nickname: (result.nickname == null ? null : result.nickname) };
}

export function describe(user) {
  return lucentCall(() => native.describe({ ...user, nickname: (user.nickname == null ? undefined : user.nickname) }));
}
