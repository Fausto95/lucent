# Swift handwritten baselines

Add one subdirectory per workload (for example `camera-luminance/`) with the
reference `.swift` sources and a short `TOOLCHAIN.md` listing `swiftc` / Xcode
versions used for the measurement.

See the parent [`README.md`](../README.md) and
[`scripts/bench-compare.md`](../../../../scripts/bench-compare.md).
