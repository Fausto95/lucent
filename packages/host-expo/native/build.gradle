plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = '{{androidPackage}}'
version = '0.0.0'

android {
  namespace "{{androidPackage}}"
  defaultConfig {
    versionCode 1
    versionName "0.0.0"
  }
  lintOptions {
    abortOnError false
  }
}
