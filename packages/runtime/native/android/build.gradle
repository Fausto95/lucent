// Generated by Lucent. Do not edit.
// The Java Lucent's Android platform code needs (dev.lucent.NativeProxy); the
// C++ is built into the app's appmodules library (CMakeLists.txt).
apply plugin: "com.android.library"

def safeExtGet(prop, fallback) {
  rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}

android {
  namespace "dev.lucent"
  compileSdk safeExtGet("compileSdkVersion", 36)
  defaultConfig {
    minSdk safeExtGet("minSdkVersion", 24)
    // Written by lucent build: what the JNI glue finds by name, kept from the app's shrinker.
    consumerProguardFiles "consumer-rules.pro"
  }
}

dependencies {
  implementation("com.facebook.react:react-android")
}
