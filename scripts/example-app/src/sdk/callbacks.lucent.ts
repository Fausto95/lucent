// Generated by scripts/sync-examples.ts from scripts/example-app. Do not edit.
// The platform calling Lucent back: a block (iOS) or a Java interface a
// function implements (Android), called on the main thread.
import { PLATFORM } from "lucent:platform";
import { OperationQueue } from "lucent:ios/Foundation";
import { Handler, Looper } from "lucent:android/android.os";

/** Resolves with what the callback saw, once the platform calls it. */
export function mainThreadCallback(): Promise<string> {
  return new Promise((resolve) => {
    if (PLATFORM === "ios") {
      OperationQueue.main.addOperation(() => resolve("called back"));
    } else {
      const looper = Looper.getMainLooper();
      if (!looper) return resolve("no main looper");
      new Handler(looper).post(() => resolve("called back"));
    }
  });
}
