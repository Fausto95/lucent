{
  "name": "lucent",
  "version": "0.0.3",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "build": "vp run --filter \"./packages/*\" build",
    "dev": "vp run @lucent-lang/lucent#dev",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check": "vp check && pnpm typecheck",
    "fix": "vp check --fix",
    "test": "vp test",
    "test:runtime": "packages/runtime/test/run.sh",
    "test:e2e": "node packages/compiler/test/e2e/run.ts",
    "website": "pnpm --filter @lucent-lang/website dev",
    "website:build": "pnpm --filter @lucent-lang/website build",
    "lucent": "vp run @lucent-lang/lucent#build >&2 && node packages/lucent/bin/lucent.cjs",
    "changeset": "changeset",
    "version-packages": "changeset version && vp fmt",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.1",
    "@changesets/cli": "^3.0.3",
    "@types/node": "^24.7.0",
    "typescript": "~5.9.3",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@9.15.9",
  "pnpm": {
    "overrides": {
      "vite": "catalog:"
    },
    "peerDependencyRules": {
      "allowAny": [
        "vite"
      ]
    }
  }
}
